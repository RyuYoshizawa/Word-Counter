# Word_Counter 仕様書

## 1. 概要

KH Coder（日本語テキストマイニングツール）の使用実績のうち、実際によく使う機能に絞って再現する独自のStreamlitアプリ。姉妹プロジェクト「After Coder」（`C:\Users\ryu\Documents\after-coding-demo\`）とアーキテクチャ・設計思想を揃え、将来的な連携を見据える。

## 2. 全体構成（ファイル構成）

```
app.py            薄いオーケストレーター（page_config, session_state初期化, sidebar呼び出し, tabs振り分け）
main.py           uv init 定型（未使用）
llm_client.py     After Coderからコピーしたプロバイダ非依存LLM呼び出しモジュール
core/             分析ロジック（Streamlit非依存、単体テスト可能）
  tokenizer.py      Sudachiラッパー（Mode A/C、強制抽出の保護/復元、複合語検出、表記ゆれ適用）
  pos_rules.py      Sudachi品詞タグ → カテゴリのマッピング、既定除外セット
  frequency.py      単語頻度、品詞別語彙リスト、複合語頻度
  network.py        共起行列、Jaccard係数（単語×単語・単語×属性値）、networkxグラフ構築（可視化は含まない）
  network_viz.py    networkxグラフ → JSON化可能な辞書への変換（色・サイズ等のエンコーディングはui側の責務）
  clustering.py     Jaccard距離、scipy linkage/dendrogram
  wordcloud_gen.py  wordcloud.WordCloudラッパー
  codebook.py       コードブック方式のコーディング集計（正規化語一致 or 原文部分文字列一致、出現率・共起/クラスター分析への橋渡し）
  variant_grouping.py  LLM表記ゆれ統合プロンプト/スキーマ
  fonts.py          日本語フォント解決（wordcloud・matplotlibで共有）
  project.py        プロジェクトファイル（.json）のシリアライズ/デシリアライズ
ui/               画面（core/の結果をStreamlitで表示するだけ）
  sidebar.py        設定サイドバー（プロジェクト管理、入力方法3択：テキストファイル/貼り付け/Excel）
  network_component.py  共起ネットワークのD3.js可視化HTML生成（st.iframeに渡す文字列を組み立てる）
  network_template.html D3.jsのforce-directedグラフ本体（HTML/CSS/JS、プレースホルダ差し替え式）
  tab_*.py          機能別タブ
                  （日本語フォントはWindows標準搭載のNotoSansJP-VF.ttf等を実行時に解決、同梱ファイルなし）
data/
  pn_ja.dic         単語感情極性対応表（cp932）
  vendor/d3.v7.min.js  D3.js本体（CDN非依存でローカルバンドル、network_component.pyがインライン埋め込み）
```

**注**: 当初計画していた`core/jobs.py`（汎用バッチジョブ状態マシン）は、Phase 7のLLM表記ゆれ統合が単発呼び出しで済んだため未作成（5.2節参照、必要になった時点で追加）。

## 3. 依存ライブラリ

| ライブラリ | 用途 |
|---|---|
| streamlit | UIフレームワーク |
| sudachipy + sudachidict_core | 形態素解析 |
| pandas / openpyxl | 表形式データのI/O |
| networkx | 共起ネットワークの計算（グラフ構築のみ、描画はD3.js） |
| scipy / matplotlib | クラスター分析（Ward法・Jaccard距離・デンドログラム） |
| wordcloud | ワードクラウド生成 |
| anthropic | LLM呼び出し（表記ゆれ統合） |

## 4. 機能仕様

### 4.0 プロジェクト管理

Word_Counterの作業単位は「プロジェクト」。`st.session_state['project']`が`None`の間はプロジェクト名・概要の入力欄とデータ入力セクションのみが表示され、**プロジェクト名とデータの両方が揃うまで分析タブ（品詞別語彙リスト以降）は一切描画されない**（`app.py`が`st.stop()`でロックする）。プロジェクトが始まったら、サイドバー上部にプロジェクト名を常時表示し、名前・概要はいつでも編集エキスパンダーから変更できる。

- **保存する内容**: `name`・`description`・`input_method`・`documents`（属性込み）・`dict_type`・`forced_terms`・`included_categories`・確定済み`variant_map`。
- **保存しないもの**: APIキー（セキュリティ上、`ui/sidebar.py`の他の設定と違いプロジェクトオブジェクトに含めていない）、トークナイズ結果などの計算済みキャッシュ（`documents`と設定さえあれば毎回安価に再計算でき、古い結果が残るリスクも避けられる）、未確定のLLM提案（`pending_variant_groups`）。
- **形式**: JSON（`.json`）。人が読める、`st.download_button`/`st.file_uploader`と相性が良い。`schema_version`・`saved_at`を含み、将来の形式変更に備える。実装は[core/project.py](core/project.py)の`build_project`/`serialize_project`/`deserialize_project`（Streamlit非依存）。
- **ダウンロード**: サイドバーの「💾 ダウンロード」ボタンで、その時点の全設定・`documents`を反映したJSONを取得できる（毎レンダリングで`st.session_state['project']`を最新状態に同期してからボタンを描画）。
- **再開（アップロード）**: サイドバーの「📂 既存プロジェクトを開く」から`.json`をアップロードすると復元される。ブラウザの仕様上`st.file_uploader`に元のファイルを再セットすることはできないため、**再開時は入力方法によらず一律「貼り付け」として復元する**（`documents`のテキストを改行結合してテキストエリアに事前セット）。ただし「貼り付け」の通常実装はテキストエリアの内容から毎回`documents`を作り直す（属性`attrs`もカスタムIDも失われる）ため、復元直後は`st.session_state['_restored_documents']`（元の`id`/`attrs`を保持）をそのまま使い、ユーザーがテキストエリアを実際に編集した時点で通常の行分割ロジックにフォールバックする（`ui/sidebar.py`の`_render_text_input`）。これにより、手を加えない限り属性情報を含めて完全に復元される。
- **新規開始**: 「🆕 新規開始」ボタンで`project`と関連session_stateをクリア（After Coderの既存リセットボタンと同様、APIキーは保持）。

### 4.1 データアップロード・クリーニング

サイドバーの「入力方法」で3択（テキストファイル/貼り付け/Excel）を切り替える。入力方法によらず、内部では統一形状`documents: list[{'id': str, 'text': str, 'attrs': dict}]`に変換してから後続処理（トークナイズ以降）に渡す（`ui/sidebar.py`）。テキストファイル/貼り付けは改行区切りの各行を1文書とし、`id`は`L0001`形式の自動採番、`attrs`は空dict。

#### 4.1.0 Excelアップロード（ID・属性列あり）
After Coderの「自由回答一覧 Chunk A」列マッピングUIを踏襲。Excelファイルをアップロード → 先頭5行プレビュー → 自由記述列（selectbox）・IDの列（任意、selectbox、未選択なら`L0001`形式で自動採番）・属性として使う列（multiselect、複数選択可・任意）を選択 → 「この内容で読み込む」ボタンで明示的に確定（列選択自体は都度session_stateに反映されるが、`documents`への変換はボタン確定時のみ発生し、After Coderと同じ「選択即反映ではなく明示確定」パターン）。属性値は文字列化して保持し、欠損セルは`attrs`から除外する。属性は集計・共起ネットワークでのマッピングに使われ、単語の判定自体には使われない（After Coderの`attrs`と同じ位置づけ）。実装は[ui/sidebar.py](ui/sidebar.py)の`_render_excel_upload`。

#### 4.1.1 強制抽出（Phase 2）
トークナイズ前の文字列保護方式。強制抽出リストの語句をUnicode私用領域のプレースホルダに置換 → Sudachiでトークナイズ → プレースホルダを元の語句（単一トークン、品詞カテゴリ「強制抽出」）に復元。リストの上にある語句ほど優先され、一度プレースホルダ化された箇所は後続の語句の置換対象にならない（最長一致ではなくリスト順で重なりを解決する、KH Coderの仕様を踏襲）。実装は[core/tokenizer.py](core/tokenizer.py)の`protect_forced_terms`/`restore_forced_tokens`。

#### 4.1.2 LLMによる表記ゆれ・同義語統合（Phase 7）
頻出語（品詞フィルタ適用後の出現数上位80語）をLLMに渡し、表記ゆれ・同義語のグルーピングを提案させる → After Coderの`pending_edit`と同じ「提案 → 内容表示 → ✅確定/❌キャンセル」UIで人が確認してから適用（自動適用しない）。実装は[core/variant_grouping.py](core/variant_grouping.py)（プロンプト・スキーマ・`llm_client.call_llm`呼び出し）と[core/tokenizer.py](core/tokenizer.py)の`apply_variant_map`（確定済みマッピングをトークンの`normalized`フィールドに適用、`surface`/品詞情報は変更しない）。確定した統合ルールは`st.session_state['variant_map']`に蓄積され、以降の全タブの集計に反映される（`app.py`で毎レンダリング時にキャッシュ済みトークンへ適用、再トークナイズ不要）。使用モデルは`claude-haiku-4-5`固定（コスト重視、コーディングモデルほどの精度は不要なタスクのため）。

### 4.2 形態素解析（Sudachi）
sudachidict_core使用（サイドバーでfullに切替可能）。Mode A（短単位、通常の単語）を基本とし、複合語検出にはMode Cとの差分を用いる。

### 4.3 品詞別語彙リスト・単語の出現数（Phase 1、原文コンテキスト表示はPhase 9）
Sudachi品詞タグを`pos_rules.py`でKH Coder風カテゴリにマッピング。助詞・助動詞等は既定で除外、ひらがなのみの語は「-B」区分としてまとめて除外可能にする（KH Coderの設計を踏襲）。全タブ共通で品詞フィルタの内容を`ui/common.py`の`pos_filter_caption`でキャプション表示する。

**原文コンテキスト表示（Phase 9）**: 左に「単語・品詞・出現数」の表（`st.dataframe(on_select='rerun', selection_mode='single-row')`で行クリックを検知）、右に選択語を含む原文一覧（[core/context.py](core/context.py)の`find_word_contexts`で前後20語を切り出し、該当語を太字表示）。「複合語を混在表示する」チェックボックスで複合語も同じ表に含められる（品詞カテゴリに「（複）」を付記、[core/pos_rules.py](core/pos_rules.py)の`display_category`）。

### 4.4 複合語の出現数（Phase 3、原文コンテキスト表示はPhase 9）
同一テキストをMode AとMode Cでそれぞれトークナイズし、Cのスパンが複数のAトークンにまたがる箇所を複合語候補として頻度集計。実装は[core/tokenizer.py](core/tokenizer.py)の`detect_compounds`（Sudachiの`begin()`/`end()`文字オフセットで両モードの範囲を突き合わせる）。強制抽出済みの語句はプレースホルダ1文字としてMode A/C双方に同一に現れるため、複合語候補としては検出されない。複合語も実際のSudachi品詞（`is_compound=True`、品詞情報自体は保持）を持つため、他タブと同じ品詞フィルタが適用される。単語一覧タブと同じクリック→原文コンテキスト表示に対応（前後の文脈にはMode Cでの通しトークン列を使う——複合語だけの抜粋では前後の普通の単語が分からないため）。

### 4.5 ワードクラウド（Phase 1、マスク/色分け/太字/高解像度出力はPhase 10）
`wordcloud`ライブラリ。日本語フォント未指定だと文字化け（豆腐）するため、[core/fonts.py](core/fonts.py)の`resolve_japanese_font`でフォントを解決する。Windows標準搭載の`C:\Windows\Fonts\NotoSansJP-VF.ttf`（無ければ`meiryo.ttc`→`msgothic.ttc`の順にフォールバック）を利用し、無断でのフォントファイルダウンロード・同梱は行わない。同じ関数をクラスター分析のmatplotlibデンドログラム（4.7節）でも共有している。

**マスク形状（Phase 10）**: 円・正方形・長方形・横長楕円・ひし形の5種類（[core/mask_shapes.py](core/mask_shapes.py)、PILで生成、白=除外/黒=描画可能というwordcloudの慣例に合わせる）。

**品詞別4色塗分け（Phase 10）**: After Coderの`CHART_COLORS`（10色）から4色を選び、それぞれに品詞カテゴリを割り当てる（[core/wordcloud_gen.py](core/wordcloud_gen.py)の`make_pos_color_func`、語→カテゴリの対応は`core/frequency.py`の`word_category_map`）。

**モノクロ3段階ネガポジ（Phase 10、実験的機能）**: 単語感情極性対応表（高村ら、`data/pn_ja.dic`、cp932、55125語）による辞書ベースの判定。[core/sentiment.py](core/sentiment.py)の`lookup_polarity`で語の極性値（-1〜1）を引き、しきい値（スライダーで調整可）でポジ＝グレー・ネガ＝ブラック・ニュートラル（辞書に無い語を含む）＝ライトグレーの3色に区分する（`make_sentiment_color_func`）。文脈を考慮したLLM判定（回答単位でのネガポジ）は将来の拡張候補（7節）。

**太字表示（Phase 10）**: NotoSansJP-VF.ttf（可変フォント）はwordcloudの`font_path`が都度読み込むため実行時のウェイト切り替えが効かない。fontToolsの`varLib.instancer`でweight=700の静的インスタンスを生成しテンポラリディレクトリにキャッシュする（[core/fonts.py](core/fonts.py)の`resolve_bold_japanese_font`）。非可変フォント（meiryo.ttc等）は隣接するBold版ファイル（meiryob.ttc）があればそれを使う。

**高解像度出力（Phase 10）**: `WordCloud(scale=...)`で標準/2倍/4倍を選択し、PNGバイト列として`st.download_button`で提供（`core/wordcloud_gen.py`の`to_png_bytes`）。

### 4.6 共起ネットワークマップ（Phase 4、属性マッピング・バグ修正・デザイン刷新はPhase 11）
文書（行 or Excel行）ごとの語の出現有無からJaccard係数を計算、networkxでグラフ構築、Plotlyでインタラクティブに描画。実装は[core/network.py](core/network.py)。サイドバーの「表示設定」で最低出現文書数・対象語数（文書頻度上位）・最大エッジ数・表示幅/高さ（Phase 11で追加）を調整可能。

**単語×属性値マッピング**: Excel入力で属性列が指定されている場合、「属性でマッピング」selectboxが現れる。属性値を単語と同じ土俵の追加ノードとして扱い、語×属性値のJaccard係数エッジも計算する（属性値どうしのペアは計算しない——語を介した関係性の可視化に絞る設計判断）。

**Phase 11でのバグ修正**: (1) 語×語エッジと語×属性値エッジを同じ`max_edges`打ち切りプールで競わせると、属性値エッジ（候補数が少なくJaccard係数も低くなりがち）が埋もれて消える不具合があった（実データで属性値ノードが1個しか出ない事例）→ `build_cooccurrence_edges`が語×語エッジと語×属性値エッジを別々のリストで返し、`build_graph`は語×語エッジのみ`max_word_edges`で絞り込み、語×属性値エッジは全て採用するよう変更。(2) ノードサイズが`10 + freq*2`という無制限の線形式で、出現文書数が多いノードが異常に巨大化する不具合があった → グラフ内最大頻度に対する相対スケール（12〜40px）に変更。

**Phase 11でのデザイン刷新（KH Coderの共起ネットワーク出力を参考）**: 属性値ノードは赤い四角（`#F08080`、`symbol='square'`）。単語ノードは接続している属性値ノードの種類数（Degree）で色分け（0=青、1=オレンジ、2=黄緑、3以上=ティール）。ノードサイズの凡例（ダミートレースでFrequency目盛りを表示）。エッジはJaccard係数で4段階（四分位）にビン分けし太さを変え、各エッジ中点に係数の数値ラベルを表示（トグルで切替可）。属性値ノードが無い場合は単一色・凡例なしの従来表示のまま（完全に後方互換）。実装は[core/network.py](core/network.py)の`_node_trace`/`_bin_edges_by_weight`/`_size_legend_traces`。

### 4.7 クラスター分析（Phase 5、文字サイズ調整・PNG/SVGダウンロードはPhase 12）
Ward法 + Jaccard距離（scipy.cluster.hierarchy）。共起ネットワークと同じ語×文書の出現有無行列を使用。デンドログラムはmatplotlib（`st.pyplot`）で表示、色分けはクラスタ数の目安から閾値を逆算。実装は[core/clustering.py](core/clustering.py)。Ward法は本来ユークリッド距離が前提だが、KH Coderと同じ組み合わせ（Ward＋Jaccard）を再現性のため踏襲している。

**Phase 12での追加**: 文字サイズスライダー（matplotlib標準の`leaf_font_size`）。PNG（DPI 150/300/600から選択）・SVG（ベクター形式、Illustrator等で編集可能）のダウンロードボタン（`figure_to_png_bytes`/`figure_to_svg_bytes`）。

### 4.8 コーディング集計（Phase 6は外部GTファイル方式、Phase 13でコードブック方式に刷新、以後「コーディング集計」に改名・出現率/共起/クラスター分析を統合）
**Phase 13で全面刷新**: 当初（Phase 6）は`document_id, category`形式の外部CSV/Excelをインポートする方式だったが、ユーザーの実際の想定（単語・語群にコードブックを策定して割り振り、既存の属性=Excel由来の`attrs`とクロス集計したい）と乖離していたため、外部ファイル方式（[core/crosstab.py](core/crosstab.py)）を削除し、アプリ内でコードブックを作る方式（新規[core/codebook.py](core/codebook.py)）に置き換えた。

**コードブックの書式**: 「コード名: トリガー1, トリガー2, ...」（1行1コード、`parse_codebook`）。**トリガーの判定は2種類を自動判定**——(1) トリガーが正規化語（`dictionary_form`）の集合に含まれていれば正規化語一致（例: 「教師: 先生, 教員, 教師」でこの3語のいずれかを含む文書がヒット、表記ゆれ・同義語のグルーピングに使う）、(2) それ以外は原文への部分文字列一致（例: 「食べない: 食べない」と「食べたい: 食べたい」は形態素解析の正規化ではどちらも「食べる」に統合されてしまうため、原文の文字列一致で活用の違いを区別する——ユーザーとの相談で「妥協」として合意した設計）。`apply_codebook`が両方式を実装。品詞フィルタ・ストップワードの対象外——常に全語を対象に判定する。

**「コーディング集計」への改名と機能統合（後述の changelog 参照）**: タブ名を「クロス集計」から「コーディング集計」に変更し、コードの概念を「表記ゆれ」より広い意味的グルーピング（例:「年配」に「年配／ベテラン／年長／経歴の長い／年寄り／高齢／経験豊か」）として再定義。同一タブ内に4セクションを統合: ①出現率（`code_occurrence_rates`）、②属性クロス集計（既存`crosstab_codes_by_attr`、属性が無い場合のみこのセクションを省略——他3セクションは属性の有無に関わらず表示）、③コードの共起分析（コードを通常の語と混ぜて分析——`core/network.py`の`attr_doc_sets`引数へ`label_codes`で衝突防止記号（†）を付けたコードを渡し、既存の属性値ハブと全く同じ仕組みで二部グラフを描く）、④コードのクラスター分析（`code_doc_matrix`＋既存`build_word_doc_matrix`の行列を結合、既定で「語も含める」——コード同士のみの比較は②③④のうち②のみが「量的比較」の性質を持つため）。

## 5. 裏側の共通基盤

### 5.1 LLM呼び出し（llm_client.py）
After Coderから流用。`make_client`/`call_llm`/`_call_anthropic`/`_parse_json_text`/`calc_cost_jpy`。プロンプトキャッシュ（`cache_control: ephemeral`）、temperature再試行`[0, 0.5]`。Word_Counter固有の変更なし。

### 5.2 バッチジョブ状態マシン（未使用）
1リランにつき1バッチ処理 → `st.rerun()` → 進捗バー＋中断ボタン。Streamlitの同期実行モデルで中断可能なバッチ処理を実現する唯一の方法（After Coderで確立されたパターン）。Phase 7では表記ゆれ統合の候補語を80語に絞り込み単発のLLM呼び出しで完結させたため、このジョブ状態マシンは未使用（7節「今後の計画」参照）。

### 5.3 pending_edit 提案/確認/キャンセルパターン（Phase 7で使用）
LLM提案を`st.session_state['pending_variant_groups']`に保持し、内容表示 → 「✅ 確定する」/「❌ キャンセル」ボタンで人が確認してから`variant_map`に適用。After Coderの`result['pending_edit']`と同じ設計思想（LLM提案を自動適用しない）。

## 5.4 文書リスト中心のトークン化（`documents` → `doc_tokens`）

サイドバーは入力方法（テキストファイル/貼り付け/Excel）によらず統一形状`documents: list[{'id','text','attrs'}]`を返す（4.1節）。`app.py`はこれを唯一の入力源とし、**文書ごとに**`protect_forced_terms`→`tokenize`→`restore_forced_tokens`を行って`doc_tokens`（文書ごとのトークンリスト）を構築する。プレースホルダは文書ごとに独立処理してよい（U+E000から文書内で完結するため文書間の衝突はない）。Phase 1〜3の集計（品詞別語彙リスト、単語の出現数、複合語）が使う全体向けの`tokens`は、`doc_tokens`を連結して導出する（かつてはテキスト全体を別途まるごとトークナイズしていたが、この二重トークナイズは2026-08-13にExcel対応と合わせて解消した）。複合語検出（`detect_compounds`）は全文書の保護済みテキストを改行で結合したものに対して1回だけ実行する（文書境界をまたいだ複合語は通常発生しないため、結合前の挙動と同じ）。`doc_ids`・`doc_attrs`も`documents`から直接導出され、共起ネットワーク（4.6節）の属性マッピングやGTクロス集計（4.8節）で使われる。

## 6. 開発の背景・設計判断メモ

- **形態素解析エンジン**: MeCabではなくSudachiを採用。複合語（Mode A/C差分）の扱いに強みがあるため。
- **強制抽出の実装方式**: Sudachiユーザー辞書（ビルドが必要）ではなく文字列保護方式を選択。Streamlitの「編集したらすぐ再実行」というUXに合わせるため。
- **共起ネットワークの描画**: pyvis（iframe埋め込み）ではなくPlotly（After Coderと同じスタック）を選択。テーマの一貫性とインタラクティブ性を両立。

## 7. 今後の計画

- TermExtract相当の統計的複合語抽出（Mode A/C差分で拾えない新語対応）
- 強制抽出リストのセッション間永続化
- Excelレポートのエクスポート（After Coderの`create_excel`パターンを参考）
- After Coderとの実データ連携（GTインポートの自動変換アダプタ）
- 表記ゆれ統合の候補語数が大きく増えた場合（数百語規模）のバッチジョブ状態マシン対応（5.2節、現状は80語の単発呼び出しで十分なため未実装）
- LLM表記ゆれ統合の実APIキーによるライブ動作確認（この開発環境にAnthropic APIキーが無いため、プロンプト構築・スキーマ・`variant_map`適用ロジックはスクリプトでモック検証済みだが、実際のLLM応答での動作は未確認 — After Coderと同じ「ブラウザE2Eは実際のAPIキーが無いと検証できない」という制約を踏襲）

## 8. 変更履歴

- 2026-08-12: プロジェクト開始。Phase 0（土台構築）完了：`uv init`、依存関係インストール（streamlit, sudachipy, sudachidict_core, pandas, openpyxl, networkx, plotly, scipy, matplotlib, wordcloud, anthropic）、`llm_client.py`をAfter Coderからコピー、`core`/`ui`のディレクトリ構成、`app.py`の骨組み。
- 2026-08-12（続き）: Phase 1（MVP縦スライス）完了。アップロード（.txt/貼り付け）→クリーニング→Sudachi Mode A解析→品詞マッピング→品詞別語彙リスト・単語の出現数・ワードクラウドが一気通貫で動作。ブラウザで実際に日本語テキストを入力して動作確認済み。
  - 単語の見出し語には`normalized_form()`ではなく`dictionary_form()`を採用。検証中に`normalized_form()`が「とても→迚も」「また→又」「いろいろ→色々」「たくさん→沢山」「すぐ→直ぐ」「やっぱり→矢張り」のように常用外の漢字表記へ変換することを発見（動詞・形容詞の活用統合自体は両者とも同じ結果）。`dictionary_form()`なら活用統合はそのままに、この望ましくない漢字化が起きないため切り替えた（[core/tokenizer.py](core/tokenizer.py)）。
  - ワードクラウド用の日本語フォントはWindows標準搭載の`NotoSansJP-VF.ttf`を実行時に解決する方式とし、フォントファイルの同梱・ダウンロードは行わなかった。
- 2026-08-12（続き）: Phase 2（強制抽出）完了。`core/tokenizer.py`の`protect_forced_terms`/`restore_forced_tokens`でUnicode私用領域プレースホルダによる文字列保護方式を実装、サイドバーに強制抽出リストUI、データ準備タブに検出結果（見つかった/見つからなかった語句）のサマリを追加。優先順位（リストの先頭が勝つ、最長一致ではない）・未検出語句の扱いをスクリプトで検証後、ブラウザでも実データにて動作確認。
- 2026-08-12（続き）: Phase 3（複合語の出現数）完了。`core/tokenizer.py`の`detect_compounds`でMode A/C差分による複合語検出を実装、`core/frequency.py`に`compound_frequency_table`を追加、複合語タブに接続。強制抽出との組み合わせ（強制抽出済みの語は複合語として重複検出されないこと）をスクリプトで検証後、ブラウザでも実データにて動作確認。
  - 開発メモ: この環境では`uv run streamlit run`のホットリロードが新規関数の追加を正しく拾わないことがあり（`ImportError`が発生）、`__pycache__`削除＋サーバー再起動で解消することを確認。以降、コア関数を追加した際はサーバーを再起動してから検証する。
- 2026-08-12（続き）: Phase 4（共起ネットワークマップ）完了。文書（行）単位のトークン化基盤（`_doc_tokens_cache`/`_doc_ids_cache`、5.4節参照）を新設し、`core/network.py`でJaccard係数の計算・networkxグラフ構築・Plotly図への変換を実装。5文書のサンプルデータでJaccard係数の計算結果をスクリプト検証後、ブラウザでも実データ（5文書・改行区切り）で動作確認（ノード数6/エッジ数14）。
- 2026-08-12（続き）: Phase 5（クラスター分析）完了。`core/clustering.py`でWard法＋Jaccard距離の階層的クラスタリング（scipy）とデンドログラム描画（matplotlib）を実装。検証中、matplotlibの既定フォント（DejaVu Sans）が日本語グリフを持たずラベルが文字化けすることが判明したため、ワードクラウドと共通の`core/fonts.py`（新設、`wordcloud_gen.py`からフォント解決ロジックを切り出し）を使ってNotoSansJP-VF.ttfをmatplotlibに登録する修正を実施。修正前後で警告の有無をスクリプトで比較検証、実際に生成した画像で日本語ラベルの表示も目視確認してからブラウザでも動作確認（対象語数7/文書数6）。
- 2026-08-12（続き）: Phase 6（GTとのクロス集計）完了。`core/crosstab.py`で`document_id`（行番号ベース）の正規化とGTインポート・クロス集計を実装。数値/文字列/`L`接頭辞ありなしの各表記でのID正規化、列不足時のエラー、実際の集計結果をスクリプトで検証。ブラウザ側はファイル選択ダイアログの自動操作が困難なため、アップロード前の空状態表示とエラーなく動作することのみ確認（パース・集計ロジック自体はスクリプト検証で担保）。
- 2026-08-12（続き）: Phase 7（LLM表記ゆれ統合）完了。`core/variant_grouping.py`（プロンプト・スキーマ・`llm_client.call_llm`呼び出し）と`core/tokenizer.py`の`apply_variant_map`を実装、`tab_cleaning.py`にpending_edit型の提案/確認/キャンセルUIを追加、サイドバーにAPIキー入力欄を新設。`app.py`側では確定済み`variant_map`を毎レンダリング時にキャッシュ済みトークン（`tokens`/`compounds`/`doc_tokens`）へ適用する設計とした（再トークナイズ不要）。この開発環境にAnthropic APIキーが無いため、`groups_to_variant_map`（canonical自己参照の除外を含む）・`apply_variant_map`・プロンプト構築をモックデータでスクリプト検証し、ブラウザでは「APIキー未設定時にボタンが正しく無効化され、クリックしても何も起きない」ことを確認。実LLM応答での動作は未検証（7節）。
- 2026-08-13（新セッション）: ユーザーが実際にアプリを試し、「初号でここまでできるのはすごい」と好評。新要望として、After Coderと同様のID・属性列付きExcelアップロード＋共起ネットワークでの単語×属性マッピングを依頼された。EnterPlanModeで設計（After Coderの`app.py:2141-2220`「自由回答一覧 Chunk A」列マッピングUIを調査した上で計画）。実装内容：
  - **アーキテクチャ変更**: サイドバーが入力方法（テキストファイル/貼り付け/Excel）によらず統一形状`documents: list[{'id','text','attrs'}]`を返すよう`ui/sidebar.py`を全面改修（3択`st.radio`、Excel分岐は列マッピング＋明示確定ボタン）。`app.py`のトークナイズ処理を`documents`中心に再構成し、従来「テキスト全体を別途まるごとトークナイズ」＋「行ごとに再トークナイズ」していた二重処理を解消（5.4節）。
  - **共起ネットワークの属性マッピング**: `core/network.py`の`build_cooccurrence_edges`に`attr_doc_sets`引数を追加し、語×属性値のJaccardエッジを計算できるようにした（属性値どうしのペアは対象外——語を介した関係性の可視化に絞る設計判断）。新規ヘルパー`attr_value_doc_sets`。`to_plotly_figure`は単語（青丸）と属性値（オレンジ菱形）を別トレースで描画し、属性値が存在する場合のみ凡例表示（属性なし時は完全に後方互換）。`tab_network.py`に属性選択selectboxを追加。
  - **検証**: `attr_value_doc_sets`・拡張後の`build_cooccurrence_edges`（word-word/word-attrエッジの存在、attr-attrペアが無いこと）をスクリプト検証。Excel→documents変換ロジック（欠損値の扱い、ID自動採番）を再現テスト。実際にxlsxファイルを書き出し→読み込み→documents変換→トークナイズ→属性付きネットワーク計算まで一気通貫の結合テストをスクリプトで実施、全て成功。ブラウザでは（1）貼り付け入力の回帰確認（データ準備タブが新しい`documents`ベースの文書数/合計文字数表示に切り替わっていることも含む）、（2）Excel選択時の列マッピングUIがエラーなく表示されることを確認（ファイル選択ダイアログの自動操作は引き続き不可のため、実アップロードのブラウザ確認はユーザー自身に委ねる）。
- 2026-08-13（続き）: ユーザーが実際に属性付きExcelをアップロードしたところ`sudachipy.errors.SudachiError: Input is too long, it can't be more than 49149 bytes`でクラッシュ。原因：`app.py`の複合語検出が全文書の保護済みテキストを改行で結合した1本の巨大な文字列を`tok.tokenize()`に一括で渡していたが、**SudachiPyは1回のtokenize呼び出しにつき入力バイト数の上限（約49149バイト、UTF-8換算）を持つ**——スクリプト検証だけで使っていたサンプルテキストは短く、この制限に気づけなかった。Excelの行数が増えると結合テキストが上限を超えて実データで初めて顕在化する典型的な「テスト規模と実データ規模の差」バグ。
  - **修正**: [core/tokenizer.py](core/tokenizer.py)に`_chunk_text_for_sudachi`（安全マージンを取った45000バイト単位で文字境界に沿って分割）を新設し、`tokenize()`・`detect_compounds()`の両方に適用——`detect_compounds`だけでなく、文書ごとに呼ばれる`tokenize()`自体も1文書・1行が極端に長い場合に同じ例外を起こしうるため、根本原因（Sudachiの入力上限という下位ライブラリの制約）に対して両関数を防御的に修正した。分割位置をまたぐ複合語は検出されなくなるが、これは元々存在した「文書境界をまたぐ複合語は検出しない」という設計上の許容範囲と同じ性質の制約。
  - **検証**: 通常サイズのテキスト（1チャンクのまま、分割前と同じ出力になること）と、元のエラーを再現する135000バイトのテキスト（3チャンクに分割され、全チャンクが上限以下、トークナイズ・複合語検出ともにクラッシュしないこと）をスクリプトで確認。
- 2026-08-13（続き）: ユーザーから「プロジェクト」概念の導入を依頼——作業状態を伝える単位が無く、中断・再開もできなかったため。EnterPlanModeで設計（ユーザーの希望を反映し2点確認：①プロジェクト名未入力時は分析タブもロック、②再開時の入力方法保持は不要で「貼り付け」相当への復元で可）。実装内容（4.0節に詳細）：
  - 新規[core/project.py](core/project.py)：`build_project`/`serialize_project`/`deserialize_project`（JSON、`schema_version`検証、APIキー・計算済みキャッシュ・未確定LLM提案は保存対象外）。
  - `ui/sidebar.py`にプロジェクト状態管理を追加：未作成時は「既存プロジェクトを開く」アップロードと名前/概要入力欄、作成後はヘッダー表示・編集・ダウンロード・新規開始ボタン。復元時は`_restored_documents`/`_restored_joined_text`の仕組みで、テキストエリア未編集の間だけ元の`id`/`attrs`を保持した`documents`をそのまま使う（編集した瞬間に通常の行分割にフォールバック）。ついでに、もう使われていなかった`raw_text`関連の配線（`settings['raw_text']`、`st.session_state['raw_text']`）を削除。
  - `app.py`に`if st.session_state.get('project') is None: st.stop()`のロックを追加。
  - **検証**: `serialize_project`/`deserialize_project`の往復・不正入力（壊れたJSON、キー欠損、未来のschema_version）をスクリプトで確認。復元時のテキスト結合・編集検知ロジックもスクリプトで再現テスト。ブラウザでは実際にプロジェクト名+テキストを入力→分析タブがロック解除されることを確認、ダウンロードボタンをクリックして実際にブラウザのネットワークリクエストからJSONの中身を取得し内容が完全に正しいことを確認、「新規開始」ボタンで初期状態に戻ることも確認。アップロードによる実際の再開操作はファイル選択ダイアログの自動操作が引き続き不可のため、ロジックのスクリプト検証に留める。
- 2026-08-13（続き）: ユーザーが実際にダウンロードしたプロジェクトファイル（338文書、教員アンケートの実データ、Excel由来・強制抽出/表記ゆれ統合ルール込み）で「読み込んで再開ができない」と報告。ファイル自体を直接読み込んで検証したところJSONとして完全に正常（`deserialize_project`も問題なく通る）——原因はファイルではなくコード側にあった。
  - **原因**: `st.file_uploader`はアップロード済みファイルをリラン後も保持し続ける仕様のため、`_render_project_start`内で「ファイルがある→復元処理→`st.rerun()`」という条件分岐をファイルの有無だけで判定していると、rerun後も同じファイルが検出され続けて復元処理が繰り返され、`st.session_state['project']`が実際に作られる（`_sync_or_create_project`はこの関数より後で呼ばれる）前に無限ループしてしまう。ボタン（`st.button`はクリックの瞬間だけTrueを返す）で確定させていたExcel読み込みフロー（4.1.0節）には無かった、file_uploaderの状態永続性特有の不具合。
  - **修正**: [ui/sidebar.py](ui/sidebar.py)の`_render_project_start`で、`UploadedFile.file_id`（アップロードごとに一意なID、Streamlit内部実装で確認）を`st.session_state['_loaded_project_file_id']`に記録し、同じfile_idの間は復元処理をスキップするよう修正。新規開始時のリセット対象にも追加。
  - **検証**: ユーザー提供の実ファイルを直接`deserialize_project`に通し、338文書・強制抽出リスト・表記ゆれ統合ルールまで含めて正しくパースされることを確認（バグはファイルではなくコード側にあったことの裏付け）。修正後、クリーンな`streamlit run`起動でエラーが無いことを確認。実際のアップロード操作でのループ再現・解消は、ファイル選択ダイアログの自動操作が不可なためユーザーによる再確認待ち。
- 2026-08-13（続き）: ユーザーが実際にプロジェクトファイルの復元まで確認し、続けて各データ加工タブへの詳細な要望を一括で伝えた。EnterPlanModeで5フェーズ（Phase 9〜13）に分けて設計・実装。

**Phase 9（単語一覧・複合語一覧の原文コンテキスト表示）**: 複合語検出を全文書結合の1本のリストから文書ごとの検出（`doc_compounds`）に変更（原文コンテキスト表示に文書境界の情報が必要なため）。`Token`に`is_compound`フィールドを追加し、複合語も実際のSudachi品詞を保持するよう変更（`pos_rules.display_category`で表示名にのみ「（複）」を付記、フィルタ判定は基本カテゴリのまま）。新規`core/context.py`の`find_word_contexts`で前後20語のコンテキスト抽出。複合語一覧の原文表示にはMode Cでの通しトークン列（`doc_tokens_mode_c`、新規キャッシュ）を使う設計に落ち着いた（`doc_compounds`は複合語だけの抜粋のため前後の普通の単語が分からない、という初期設計の見落としに気づいてその場で修正）。

**Phase 10（ワードクラウド拡張）**: マスク5形状、After Coderの`CHART_COLORS`から品詞別4色塗分け、ユーザー提供の単語感情極性対応表（`pn_ja.dic`、cp932、55125語、`data/`に同梱）によるモノクロ3段階ネガポジ（実験的機能、後日ユーザーから追加要望のあった閾値調整可能な3段階版）、太字表示（可変フォントをfontToolsで静的Bold instanceに変換）、高解像度PNGダウンロード。

**Phase 11（共起ネットワークの修正とデザイン刷新）**: ユーザーが送ってくれたスクリーンショット2枚（現状の不具合＝属性値ノードが1個しか出ず巨大化、希望＝KH Coder風の見た目）を元に診断・設計。語×語エッジと語×属性値エッジの打ち切りプールを分離するバグ修正、ノードサイズの相対スケール化、属性値ノード=赤い四角、単語ノード=Degree（接続属性数）で色分け、サイズ/エッジ太さの凡例、エッジ数値ラベル、表示サイズ指定を実装。

**Phase 12（クラスター分析の出力オプション）**: 文字サイズスライダー、PNG（DPI指定）/SVG（ベクター）ダウンロード。

**Phase 13（クロス集計のコードブック方式への刷新）**: ユーザーから「想定イメージがズレていると思う」と説明を受け、当初の外部GTファイル方式から、アプリ内でコードブックを作りExcel由来の属性とクロス集計する方式に全面刷新。「食べない」「食べたい」のように形態素解析の正規化で区別が消える活用差への対応が課題として挙がり、ユーザーと相談の上「トリガーが正規化語なら見出し語一致、それ以外は原文への部分文字列一致」という自動判定の妥協案で合意（`core/codebook.py`）。旧`core/crosstab.py`は削除。

**共通**: 全タブに品詞フィルタの内容を示すキャプション（`ui/common.py`の`pos_filter_caption`）を追加。実装の過程で複合語タブが品詞フィルタを一切適用していなかった既存の不整合（Phase 3由来）に気づき、`compound_frequency_table`に`included_categories`引数を追加して修正した。

**検証**: 各`core/*.py`の新規/変更関数はスタンドアロンスクリプトで検証（`find_word_contexts`の前後20語切り出し、`is_compound`混在時の表示、複合語のコンテキストにMode Cを使うことの妥当性、感情極性辞書の読み込みと閾値判定、マスク形状生成、太字フォントのfontTools静的インスタンス化、ネットワークの属性値ノード欠落バグ・ノードサイズ上限バグの再現と修正確認、デンドログラムのPNG/SVG出力、コードブックの正規化語一致/部分文字列一致の両方＋クロス集計表）。ブラウザでは各Phase完了ごとにクリーンな`streamlit run`起動でエラーが無いことと、新規UIコントロールが正しくレンダリングされることを確認（キャンバスベースの表クリック操作・ファイルアップロードダイアログは引き続き自動操作不可のため、それらに依存する部分はスクリプト検証で担保）。
- 2026-08-13（続き）: ユーザーが実際にPhase 9〜13を試し、細かな修正依頼を一括で受けた。
  - **出現語一覧**（旧「品詞別語彙リスト・単語の出現数」、タブ名を短縮）: 左右カラムを等幅から`st.columns([1, 2])`に変更し、原文表示側を広く確保（該当する原文の後半が読みにくいという指摘は、前後20語のトークン数自体は`find_word_contexts`をスクリプトで再検証し正確であることを確認済みで、カラム幅の問題だった可能性が高い）。「複合語を混在表示する」チェックボックスを廃止し、「表示: 単語／複合語／混在」のラジオボタンに統一（複合語タブの削除に伴う統合）。複合語のみ表示時、原文コンテキストの検索先を`doc_tokens_mode_c`（Mode Cの通しトークン列）に切り替え——複合語だけの抜粋である`doc_compounds`のまま検索すると前後の文脈が全く別の場所から拾われてしまう不具合があったため。
  - **複合語の出現数タブを削除**。`core/frequency.py`の`compound_frequency_table`（呼び出し元が無くなったため）と`core/crosstab.py`相当の旧関数も整理。`pos_rules.display_category`は複合語の品詞を基本カテゴリ＋「（複）」から、一律「（複）」表記に単純化（フィルタ判定は引き続き基本カテゴリで行う）。
  - **ワードクラウド**: マスク形状の輪郭に沿ってランダムな円を重ね描きし、雲のような凹凸のある輪郭にする`_add_cloud_bumps`を追加（[core/mask_shapes.py](core/mask_shapes.py)）。`prefer_horizontal=1.0`で縦書きを禁止。色選択UIを刷新——16進コードのselectbox＋`st.color_picker(disabled=True)`のプレビューが機能していなかったため、色見本を番号付きで常時表示する`_render_color_legend`（HTML/CSSの静的スウォッチ）に置き換え、色は番号で選択する方式にした。品詞の割当も単一selectboxから`st.multiselect`に変更し、1色に複数品詞を割り当てられるようにした。太字の既定ウェイトを700→600に変更（キャッシュファイル名にウェイト値を含め、古いキャッシュを誤って使い回さないようにした）。
  - **共起ネットワーク**: ノードのテキストラベルを`textposition='top center'`（円の外側）から`'middle center'`（円の中央）に変更し、ノードサイズも文字が収まりやすいよう拡大（12〜40px→22〜60px）。語の文字数に応じてフォントサイズを可変にする調整も追加。「表示設定」エキスパンダー内のコントロールを3列×2行＋単独行×2から、4列×2行に再配置し、縦方向のスペースをおよそ半分以下に圧縮。
  - **検証**: 各変更をスタンドアロンスクリプトで検証（前後20語のトークン数の正確性、複合語の統一表記、色の複数品詞割当、太字ウェイトのキャッシュファイル名、縦書き禁止設定、ノードのテキスト位置とフォントサイズ）。マスク形状は実際に画像として保存し目視で「雲っぽさ」を確認。ブラウザでは全タブがエラー無く新しいUIで表示されることを確認。
  - **副次的な発見**: ユーザーから「既存プロジェクトを開くしかメニューに表示されない」という報告があったが、クリーンなブラウザセッションでは再現せず、ポート8502で2プロセス確認されたものの実際は1つのStreamlitサーバーの親子プロセス（正常）と判明。今日何度もコード変更・サーバー再起動を行った影響で、開きっぱなしのブラウザタブに古いセッション状態が残っていた可能性が高いと判断し、ハードリロードを案内した。
- 2026-08-13（続き）: 本日最後の2件の要望に対応。
  - **アイコン追加**: 当初はサイドバータイトルに`👾`絵文字を連結する方式（`st.sidebar.title('👾 Word_Counter')`）で実装したが、ユーザーから姉妹アプリAfter Coderのスクリーンショットが送られ「マークが小さい、姉妹アプリと同じサイズにしてほしい」と修正依頼。After Coderの実装（`app.py`の`st.logo(str(APP_DIR / 'after_coder_MARK.png'), size='large')`）を調査したところ、絵文字テキストではなく50×32pxのピンク色スペースインベーダーPNG画像を`st.logo(..., size='large')`でサイドバー最上部に大きく表示する方式だと判明。同じ仕組みを踏襲する目的で、いったんAfter Coderのマーク画像（`after_coder_MARK.png`、ピンク色）をそのまま`word_counter_MARK.png`としてコピーし、`app.py`に`st.logo(str(APP_DIR / 'word_counter_MARK.png'), size='large')`を追加、サイドバータイトルは`st.sidebar.title('Word_Counter')`（絵文字なし）に戻してAfter Coderと同じ構成に揃えた。あわせて`app.py`の`st.set_page_config`に`page_icon='👾'`を追加し、ブラウザタブのファビコンには引き続き絵文字を使用（st.logoはファビコンには影響しないため併用）。
  - **マーク画像の差し替え**: 上記の暫定対応がAfter Coderのマーク画像をそのまま流用していたため、ユーザーから「姉妹版のマークをそのまま使っている、先に送ったファイルに置き換えて」と指摘。ユーザーが本日最初にこの機能を依頼した際に添付していた画像（水色のスペースインベーダー、50×32px PNG）をセッションのトランスクリプトファイル（jsonl）からbase64データとして復元し、`word_counter_MARK.png`を正しい画像で上書き。ファイルサイズ・寸法はAfter Coderのものと同一（`st.logo(size='large')`での見た目のサイズは揃ったまま）で、色のみ異なる。
  - **ストップワード**: サイドバーに強制抽出と同じ`st.text_area`パターンで「ストップワード」入力欄を新設（強制抽出の直後、品詞フィルタの前）。除外は正規化した見出し語（`Token.normalized`）の完全一致で行う。`core/frequency.py`の`_filter_tokens`（およびそれを使う`pos_frequency_table`/`word_frequency_table`/`word_category_map`）、`core/network.py`の`_doc_word_sets`（`build_cooccurrence_edges`経由）、`core/clustering.py`の`build_word_doc_matrix`に`stopwords`引数を追加し、出現語一覧・ワードクラウド・共起ネットワーク・クラスター分析の集計から除外されるようにした。原文コンテキスト表示（`core/context.py`の`find_word_contexts`が使う`doc_tokens`/`doc_tokens_mode_c`）とクロス集計（コードブックのトリガーは明示的な語なのでストップワードの対象外という設計判断）には適用していない。プロジェクトファイル（`core/project.py`）にも`stopwords`フィールドを追加したが、旧バージョンのプロジェクトファイルとの後方互換のため必須キーには含めず、読み込み時は`project.get('stopwords', [])`で欠損時は空リスト扱いにしている。
  - **検証**: `_filter_tokens`/`_doc_word_sets`/`build_word_doc_matrix`それぞれについて、ストップワード指定前後で対象語が期待通り増減することをスタンドアロンスクリプトで確認（例: 「天気」を4回含むサンプル文書でストップワード指定前は出現語一覧に行があり指定後は消えること、共起ネットワーク・クラスター分析の対象語集合からも除外されること）。ブラウザでも新しいサイドバーUI（ストップワード入力欄が強制抽出の直後に表示されること）とアイコン表示（サイドバータイトル・ブラウザタブ）をクリーンな`streamlit run`起動後に確認、エラー無し。

- 2026-08-14（新セッション）: ユーザーから次回作業の方向性を確認。コンセプトを「アナリストの読解センスを活かしたテキストマイニングエージェント」と定義し、出現語一覧タブ（品詞フィルタ・ストップワード変更に対するリアルタイム反映＋原文確認）を体現例として高評価。残タスクの優先順位は①クロス集計のコーディング機能、②共起ネットワークの抜本改革、③ワードクラウド・クラスター分析のグラフィック強化——今回は②から着手。

**共起ネットワークマップのD3.js全面刷新（Plotlyから移行）**: EnterPlanModeで設計（求める体験を確認: (a)出現語一覧と同じリアルタイム性、(b)見た目・デザインの刷新、(c)大規模データでの見やすさ改善——「ノード操作のインタラクティブ性」は必須要件から明示的に除外）。設計フェーズでPlanエージェントに技術検証を依頼し、重要な発見を得た: インストール済みStreamlit 1.61.1では`st.components.v1.html`が既に非推奨で、後継の`st.iframe(src, width=..., height=...)`を使うべきと判明（実機のシグネチャで確認）。

- **アーキテクチャ**: 可視化のエンコーディング（色・サイズ・エッジ太さ）を完全にJS側の責務とし、`core/network.py`はグラフ統計のみを扱う（`core/`＝Streamlit非依存という既存の設計原則を維持）。新規`core/network_viz.py`の`graph_to_json_dict(g)`がnetworkxグラフを`{nodes, links, max_freq}`のJSON化可能な辞書に変換。新規`ui/network_component.py`の`build_network_html()`が、`ui/network_template.html`（D3のforce-directedグラフ一式）のプレースホルダをテンプレート文字列置換（`str.format()`はJS/CSSの`{}`と、`string.Template`はES6テンプレートリテラルの`${...}`とそれぞれ衝突するため単純な`.replace()`を採用）で埋めてHTML文字列を組み立て、`ui/tab_network.py`が`st.iframe(html, width=width, height=height)`で埋め込む。毎rerunでHTML文字列を丸ごと再生成し新しいiframeとして描画する一方向モデル（他の全タブと同じ「毎rerunで結果を丸ごと再計算・再描画」というパターンに準拠、JS→Pythonの通信は無し、`declare_component`ベースの本格的なカスタムコンポーネントは不要と判断）。
- **D3.js本体**（v7、約280KB）はCDN参照ではなく`data/vendor/d3.v7.min.js`にローカルバンドルし、生成HTMLの`<script>`タグにインライン埋め込み——外部ネットワークリクエストを一切発生させないため、オフライン環境でも動作する。
- **可視化のデザイン刷新**: 旧KH-Coder風の配色（`_WORD_DEGREE_COLORS`の4色・`_ATTR_COLOR`の赤）を廃止し、After Coderと共有する`core/wordcloud_gen.CHART_COLORS`パレットを採用——単語ノードは接続属性数（`attr_degree`）に応じ`#83C9FF`（低）〜`#0068C9`（高）のグラデーション、属性値ノードは`#FF8700`（暖色）、属性マッピング無し時は単語一色`#0068C9`。ノードサイズは既存の22〜60px線形スケールをD3の`scaleLinear`で再現。エッジ太さ・不透明度は、旧来の四分位ビン分け（`_bin_edges_by_weight`、Plotlyのトレース分割のため必要だった）を廃止し、生のJaccard係数から連続的にスケールするよう簡素化。
- **大規模データでの見やすさ改善**: `d3.forceCollide`でノード重なりを抑制、ラベルは頻度中央値以上のノードのみ常時表示（それ以外はホバー時にツールチップ表示）、`d3.zoom()`によるズーム・パンで密集部分を拡大可能に。
- **仕様変更**: 表示設定の「エッジに係数を表示」チェックボックスの意味を「常時オンキャンバス表示」から「ホバーで係数を表示」（`show_edge_labels`引数はそのまま、ホバー時のツールチップ表示可否に用途を変更）に再定義——常時表示だと`max_edges=300`時に大規模データ目標(c)と矛盾するクラッター源になるため。
- **テーマ対応**: `st.context.theme.type`（'light'/'dark'/None）を読み取り、iframe内の背景・文字色をStreamlitの既定テーマ色に追従させる（`srcdoc`のiframeは独立ドキュメントで親ページのCSSを継承できないため、明示的に色を渡す必要がある）。
- **削除**: `core/network.py`から`to_plotly_figure`/`_node_trace`/`_size_legend_traces`/`_bin_edges_by_weight`/`_node_size`等のPlotly専用コードを全て削除（Jaccard係数計算のロジック自体（`build_cooccurrence_edges`/`build_graph`等）は変更なし）。`plotly`パッケージへの参照が本タブ以外に存在しないことをgrepで確認した上で`pyproject.toml`から依存を削除。
- **検証**: `core/network_viz.graph_to_json_dict`をスタンドアロンスクリプトで検証（`nodes`/`links`/`max_freq`の形、JSON往復）。`ui/network_component.build_network_html`もスタンドアロンスクリプトでプレースホルダの置換漏れが無いこと・light/dark両テーマで正しい配色になることを確認。実際の統合検証には、サイドバーのラジオボタンクリックがブラウザ自動操作で反応しない問題（BaseWebのカスタムラジオコンポーネント特有の挙動、既知の環境制約の新たな一例）が発生したため、一時的な検証用Streamlitハーネス（`tab_network.render()`を合成データで直接呼び出す小さなスクリプト、検証後削除）を実際に`streamlit run`で起動し、ブラウザの`iframe.contentDocument`を`javascript_tool`経由で直接クエリして検証: 単語のみのケース（5単語・10エッジ）で`<circle>`5個・`<line>`10個が生成されることを確認、属性値込みのケース（27単語・3属性値・136エッジ）で`<circle>`27個・`<rect>`3個・`<line>`136個が生成されることを確認——Python側の計算結果とD3側の描画結果が完全に一致。ノードの`fill`色（`rgb(0, 104, 201)`=`#0068C9`、属性値`#FF8700`）・半径・エッジの`stroke-width`/`stroke-opacity`も設計通りの値であることを直接確認。`read_network_requests`でd3js.org等への外部リクエストが一切発生していないことも確認。`read_console_messages`でJSエラー無し。クリーンな`streamlit run`起動後、本体アプリ（`app.py`）もエラー無く起動することを確認。

- 2026-08-14（続き）: ユーザーが実データで共起ネットワークマップを試用し、「グラフィックは魅力的だがネットワーク表現が複雑すぎる」とフィードバック。目標イメージとしてKH Coderの実際のマップ画像（`N 49, E 60, D .051`——各単語がほぼ1つの属性値ハブにのみ繋がり、3つの属性値を中心にきれいに分離したクラスターを形成）と設定パネル画像を受け取り比較。

**属性エッジの単語ごと上限を導入（根本原因の修正）**: 原因は単語×属性値エッジが一切上限なく描画されていたこと——`build_graph`は単語×単語エッジのみ`max_word_edges`で絞るが、単語×属性値エッジは候補になった組み合わせを全て採用しており、`top_n=50`×`attr_top_n=5`なら理論上250本近い属性エッジが一度に描画されうる設計だった。ユーザーと確認の上、[core/network.py](core/network.py)の`build_graph`に`max_attr_edges_per_word`引数（既定1本）を追加——各単語について係数が最も高い属性値エッジから上位K本のみを残す。単語×単語エッジのプールとは完全に独立した制限であり、Phase 11で単語×属性値エッジが単語×単語エッジのプールに埋もれて消えた不具合（プールを分離して修正済み）とは別の軸の対策のため、既存のプール分離設計はそのまま維持。[ui/tab_network.py](ui/tab_network.py)に「単語ごとの最大属性エッジ数」スライダー（属性マッピング有効時のみ表示、既定1・範囲1〜5）を追加。

**係数ラベルを常時キャンバス表示に戻す**: 前回の刷新では「エッジが最大300本まであり得る」前提で、常時表示だとクラッターになると判断し、ラベル表示をエッジのホバー時のみのツールチップに変更していた。今回の上限導入でエッジ総数が大幅に減る（KH Coderの60本規模に近づく）ため、その前提が崩れたと判断し、ユーザー確認の上でKH Coder同様「エッジ中点に係数の数値を常時描画」する方式に戻した。[ui/network_template.html](ui/network_template.html)——`<line>`のmouseenter/mousemove/mouseleaveによる係数ツールチップは削除し（ノード側のホバーツールチップ＝単語/属性値名・出現文書数・接続属性数の表示は維持）、`showEdgeCoef`がtrueの場合のみ`DATA.links`にバインドした`<text class="edge-label">`要素を追加、`simulation.on('tick', ...)`内でエッジ中点座標に追従させる。線が交差する背景でも数字が読めるよう`paint-order: stroke`＋背景色のstroke（ハロー）をCSSで付与。設定チェックボックスのラベルも「ホバーで係数を表示」から元の「エッジに係数を表示」に戻した。

- **検証**: `build_graph`の新しい単語ごと属性エッジ上限を、1つの単語が複数の属性値と弱くつながる合成データでスタンドアロンスクリプト検証——`max_attr_edges_per_word=1`で各単語の属性エッジが最強の1本のみに絞られること、`K=2`で2本、`None`で全て（上限なし）残ることをアサートし全てパス。前回同様、一時的な検証用Streamlitハーネス（`build_graph`→`graph_to_json_dict`→`build_network_html`を合成データで直接呼び出す小さなスクリプト、検証後削除）を`streamlit run`で起動し、`iframe.contentDocument`を`javascript_tool`経由でクエリして検証: 上限適用前後のエッジ数（word-word 2本＋word-attr 9本のケース）が期待通り「上限あり=5本」「上限なし=11本」になることを確認。係数ラベルON時に`<text class="edge-label">`の数がエッジ数（`<line>`）と1対1で一致すること（5個/5個）、OFF時に0個になることを確認、最初のラベルのテキストが実際のJaccard係数値（"0.40"）と一致することも確認。ノードラベルの中央値ベースの間引きロジック（今回変更していない部分）が引き続き正しく動作していること（6ノード中3ノードにラベル）も副次的に確認。`read_console_messages`でJSエラー無し。クリーンな`streamlit run`起動後、本体アプリもエラー無く起動することを確認。

- 2026-08-14（続き）: ユーザーが属性エッジ上限導入後のマップを実データで再テストし、スクリーンショット付きで4点フィードバック。①属性値ノードがforce simulation任せで散らばっており、KH Coderのような「属性値を中心に単語が放射状に広がるクラスター」になっていない、②単語ノードの半分以上にラベルが表示されておらず読みづらい（前回導入した頻度中央値以上のみラベル表示という間引きが原因）、③印刷を考慮するとダーク背景よりホワイト背景がよい、④ノードの白い輪郭線は不要。

**属性値ノードを円周上に等間隔で固定配置**: [ui/network_template.html](ui/network_template.html)で、属性値ノードにD3標準の`fx`/`fy`（force simulation中も座標を固定する仕組み、追加ライブラリ不要）を設定し、キャンバス中心を基準に属性数に応じた等間隔の円周上に配置（`angle = -90° + 360°×i/N`、半径は`min(width,height)×0.35`）。各単語は前回導入した`max_attr_edges_per_word`の上限により基本1つの属性値としか繋がっていないため、既存の`forceLink`がその単語を担当ハブの方向へ自然に引き寄せる——追加のクラスタリング用の力は不要だった。単語ノード自体はこれまで通りforce simulationで自由に動く。属性マッピング未指定時（属性値ノードが無い場合）は何も影響しない。

**ラベルを全ノードに表示、文字サイズはユーザー調整可能に**: 頻度中央値以上のみラベル表示という間引きロジックを撤廃し、全ノードに`text.node-label`を常時表示。フォントサイズはノード数依存の自動計算式をやめ、[ui/tab_clustering.py](ui/tab_clustering.py)の「文字サイズ」スライダーと同じパターンで新規スライダー（表示設定、`min_value=6, max_value=20, value=11`）から`ui/network_component.py`の`build_network_html(..., label_font_size=...)`経由で渡すよう変更。

**輪郭線の削除・配色の簡素化**: ノード（`circle`/`rect`）の白い輪郭線（`stroke`）を削除。ダークテーマ追従（`st.context.theme.type`を読んで背景色を切り替える仕組み、前回の刷新で先回りして実装していたもの）は、印刷前提が明示されたことで不要な複雑さと判断し撤去——`ui/network_component.py`から`theme_type`引数・`_DARK_BG`/`_DARK_FG`分岐を削除し、常時ホワイト背景（`#FFFFFF`）＋濃いグレー文字（`#222222`）を使う。`ui/tab_network.py`から`st.context.theme.type`の取得・受け渡しも削除。

- **検証**: 一時的な検証用Streamlitハーネス（合成データで`build_graph`→`graph_to_json_dict`→`build_network_html`を直接呼び出す小さなスクリプト、検証後削除）を`streamlit run`で起動し、`iframe.contentDocument`を`javascript_tool`経由で直接クエリして検証: 属性値ノード2個（同カテゴリで7個中5個は`max_attr_edges_per_word=1`の絞り込みで孤立しグラフから除外される合成データを用意）の`fx`/`fy`座標が、Python側で使ったのと同じ幅800×高さ600・半径`min(800,600)×0.35=210`の計算式による期待値（`(400, 90)`・`(400, 510)`）と完全一致することを確認（D3がDOM要素に紐付ける`__data__`をJS側で直接読み出して比較）。単語ノードには`fx`/`fy`が設定されていない（自由に動く）ことも確認。ノードラベル数がノード総数と一致（5/5、間引きが無くなったことの確認）、`circle`/`rect`に`stroke`属性が一切付与されていないこと、iframeの`<body>`の計算済み背景色が`rgb(255, 255, 255)`（白）であること、ラベルのフォントサイズが指定した`label_font_size`の値と一致することをそれぞれ確認。`read_console_messages`でJSエラー無し。クリーンな`streamlit run`起動後、本体アプリもエラー無く起動することを確認。

- 2026-08-14（続き）: それでもユーザーから「共起ネットワークがまだ十分ではない」とフィードバック。属性値ハブの固定配置（前回対応済み）以外の部分——単語×単語のエッジ構造——が引き続きD3の汎用的なforce simulation任せで、KH Coderのような整然とした配置に至っていないことが原因。ユーザーから[python-igraph](https://python.igraph.org/en/stable/)の導入を提案され、「まずはレイアウト計算のみ置き換え（推奨）」の方針で合意（色分けロジックは現状維持、コミュニティ検出による色分け・グルーピングは次回以降）。

**座標計算をPython（igraph）側に移設**: `uv add python-igraph`で追加（import名は`igraph`）。新規`core/network_viz.py`の`compute_layout(g, width, height)`——属性値ノードは前回同様の等間隔円周配置（式はPython側に移植）、単語ノードは単語×単語サブグラフのみを`igraph.Graph`として自前構築（`ig.Graph.from_networkx()`は使わず、ノードID⇔インデックス対応を自前管理——`from_networkx`のノード名属性の扱いがバージョン依存でドキュメントから確証を得られなかったため、リスク回避）し、`layout_fruchterman_reingold(weights=Jaccard係数, seed=円周上の初期配置)`でレイアウトを計算。各単語について、その単語が持つ属性値エッジのうち最も係数が高い相手（担当ハブ）を特定し、同じハブを担当する単語グループごとにセントロイドをハブアンカー位置へ平行移動——単語同士の相対配置（FRレイアウトの質）を保ちつつハブごとのまとまりを作る。ハブが無い場合（属性マッピング無効時）は単語サブグラフのFRレイアウトを正規化してそのまま使う。`graph_to_json_dict`は`positions`辞書を受け取り各ノードに`x`/`y`を埋め込むよう変更。
  - **実装中に見つかった不具合**: ハブへの平行移動で単語ノードがキャンバス外にはみ出すケースを検証中に発見（特に単語×単語エッジが少ない/無い場合、FRの初期正規化がキャンバス全体を使い切ってしまい、ハブへの移動後に境界を超える）。対策として、ハブがある場合は単語サブグラフの初期正規化先をキャンバスの55%サイズに縮小（平行移動の余地を残す）した上で、最終的に全単語ノードの座標をキャンバス内へクランプする安全策を追加。
  - **レイアウトの非決定性とキャッシュ**: 実機で確認したところ、`layout_fruchterman_reingold`は`seed`（初期座標）を固定して2回呼び出しても結果が一致しない（igraphのC実装内部に追加の乱数要素がある模様）。このタブと無関係なウィジェット操作でもStreamlitはスクリプト全体を再実行するため、キャッシュ無しではネットワーク図が無関係な操作のたびに再シャッフルされてしまう。[app.py](app.py)の`_tokenize_cache_key`と同じパターンで、`ui/tab_network.py`にグラフの内容（ノード・エッジ・重み・width/height）から作ったハッシュキーによる`st.session_state`キャッシュを追加（`core/network_viz.py`はStreamlit非依存の原則を保つため、キャッシュ自体はui層で行う）。
  - **D3側の簡素化**: [ui/network_template.html](ui/network_template.html)から`forceLink`・`forceManyBody`・`forceCenter`を削除し`forceCollide`（重なり回避）のみ残す。ノードの初期`x`/`y`はPython計算値をそのまま使う（D3のforceSimulationは既にx/yが設定されたノードをランダム配置しない、という標準挙動を利用）。属性値ノードは`fx`/`fy`にx/yを代入して固定。`forceLink`の除去に伴い失われる「エッジのsource/target文字列ID→ノードオブジェクト」の自動解決を、`Map`による手動解決（`DATA.links`に`sourceNode`/`targetNode`を事前設定）に置き換え、エッジ描画・tick更新・エッジラベル中点計算をこれらのプロパティ参照に修正。
- **検証**: `compute_layout`をスタンドアロンスクリプトで検証——ハブアンカー座標が計算式通りであること、各単語が実際に最も係数の高い属性値ハブに近い位置に配置されること（無関係なハブとの距離より必ず小さいことをアサート）、全座標がキャンバス範囲内に収まること、属性マッピング無し/単語×単語エッジ0本という2つの境界ケースでも例外やクランプ漏れが起きないことを確認（クランプ対策前は境界ケースで座標がy=-150までキャンバス外にはみ出す不具合を実際に検出→修正→再検証で解消を確認）。一時的な検証用Streamlitハーネス（`build_graph`→`compute_layout`→`graph_to_json_dict`→`build_network_html`を合成データで直接呼び出す小さなスクリプト、検証後削除）を`streamlit run`で起動し、`iframe.contentDocument`を`javascript_tool`経由でクエリして統合検証: 属性値ノードの`fx`/`fy`がPython計算値と完全一致、単語ノードには`fx`/`fy`が設定されていないこと、全`<line>`のx1/y1/x2/y2が有限値（`sourceNode`/`targetNode`解決が機能していることの確認）、3単語それぞれが正しい担当ハブの近く（数十px）に配置され無関係なハブとは大きく離れていることを実際のD3描画結果から確認、ノード色・輪郭線無し・背景色（白）も前回までの実装から変化していないことを確認。`read_console_messages`でJSエラー無し。クリーンな`streamlit run`起動後、本体アプリもエラー無く起動することを確認。

- 2026-08-14（続き）: igraph導入後の実データスクリーンショットで、属性マッピング無しの結果は「非常に良くなった」と好評。属性マッピング有りの方も改善したが、特定のハブ（「校長・教頭」周辺）に単語が密集して重なり合う問題を指摘。「エッジの長さは関係性には意味がないので純粋にレイアウトの問題」と分析を受け、併せて「バブルサイズは若干重なってもいいので大きく（あるいは指定できる）」「塗りつぶしでなく現在の色で丸い罫線、半透明の水色で塗りつぶしに」と要望。

**ハブグループの密集を解消（メンバー数に応じた比例拡縮）**: 原因は、全ハブグループに一律の縮尺を適用していたため、語数の多いハブ（データの偏りで大多数の単語が最も強く繋がる先になりやすい）ほど単語同士が重なり合っていたこと。[core/network_viz.py](core/network_viz.py)の`compute_layout`を修正——単語×単語サブグラフの初期正規化は常にキャンバス全体を使うよう単純化（以前の「ハブがある場合はキャンバス55%に圧縮」という一律の縮小をやめた）、代わりに各ハブグループについて重心からの目標半径を`28×√(メンバー数−1)`で計算し（メンバーが多いほど広く、少ないほどコンパクトに）、隣接ハブとの弦の長さのおよそ85%を上限にクランプ（ハブ数が多いほど1グループあたりの持ち分は狭くなる）した上で、グループを目標半径まで重心基準に拡大・縮小してからハブアンカーへ平行移動する。合成データ（15語のハブA・3語のハブB）で、Aの平均広がり半径がBより明確に大きくなることを検証済み。
- **バブルサイズの調整**: [ui/tab_network.py](ui/tab_network.py)の表示設定に「バブルサイズ」スライダー（`min_value=0.5, max_value=3.0, value=1.5`、既定値も従来より大きくした）を追加、`ui/network_component.py`の`build_network_html(..., node_size_scale=...)`経由で[ui/network_template.html](ui/network_template.html)の`sizeScale`のrange（従来`[11, 30]`）に掛け合わせる。
- **ノードの見た目を半透明塗り＋色付き輪郭に変更**: 塗りつぶし色を固定の水色（`#87CEEB`、`fill-opacity: 0.45`）に統一し、これまで塗りつぶしに使っていた色（単語は接続属性数に応じた青のグラデーション、属性値はオレンジ）は`stroke`（輪郭線、幅2px）に回すよう変更。半透明にしたことで、要望通り「多少重なっても見やすい」表示になる。
- **検証**: `compute_layout`の比例拡縮ロジックをスタンドアロンスクリプトで検証（上記の15語×3語ハブ比較、境界内に収まることも確認）。一時的な検証用Streamlitハーネス（検証後削除）を`streamlit run`で起動し、`iframe.contentDocument`を`javascript_tool`経由でクエリして新しいスタイル（`fill`/`fill-opacity`/`stroke`/`stroke-width`）とバブルサイズ倍率（`node_size_scale=2.0`指定時に`circle`の半径が期待値通り41pxになること）を確認。`read_console_messages`でJSエラー無し。クリーンな`streamlit run`起動後、本体アプリもエラー無く起動することを確認。

- 2026-08-14（続き）: ユーザーが実データ相当のスクリーンショット2枚（属性マッピング無し／属性マッピング有り）で再テスト。属性マッピング無しは「ほぼ完璧」と高評価。属性マッピング有りは、特定のハブ（多数派の属性値、この例では「一般教員（校長・教頭以外）」）が常にキャンバス上部に描かれ、密集した単語クラスターがキャンバス上端で見切れる（切れる）ことがあると指摘。「やはり属性にノードが集中します」と、前回のメンバー数比例拡縮だけでは不十分だったことも判明。追加で、左上にタイトル（共起ネットワーク＋プロジェクト名）、右側に凡例の表示も要望。

**クラスタ密集の追加緩和**: 前回導入したグループ半径の上限（隣接ハブとの弦の長さに基づく均等割り）を計算したところ、実データ相当の偏り（40語 vs 5語のハブ）では、多数派ハブの目標半径（`32×√39≈200px`）がこの均等割りの上限（3ハブ時で`hub_radius×0.736≈180px`）によってちょうど頭打ちになっていたことが判明——これが密集の直接原因。[core/network_viz.py](core/network_viz.py)の`compute_layout`で、上限をハブ数に基づく均等割りから`hub_radius×0.85`という一律の緩い値に変更（半透明塗りへの変更でクラスター同士が多少重なっても許容できるようになったことを踏まえた判断）。目標半径の係数も28→32に若干引き上げ。
- **キャンバス上端はみ出しバグの修正**: 原因は、Python側の最終クランプ（`compute_layout`内、単語ノードの中心座標を`[margin, width/height-margin]`に収める処理）が固定の小さい余白（従来20px）しか見ておらず、実際の見た目の半径（バブルサイズ倍率次第で数十px、最大約90px）を考慮していなかったこと——中心座標はキャンバス内でも、円の上端がキャンバス外にはみ出すことがあった。バブルサイズ倍率はJS側だけが知る値のため、Python側では大まかな余白（20→40px）に緩和するに留め、正確な境界処理は[ui/network_template.html](ui/network_template.html)に新設した`forceBoundary`カスタムフォース（毎tick、各単語ノードの実際の半径`sizeScale(d.freq)`を使って中心座標を`[r, width-r]`/`[r, height-r]`にクランプ）に担わせるよう修正。
- **タイトル・凡例の追加**: [ui/network_component.py](ui/network_component.py)の`build_network_html`に`title`引数を追加、[ui/tab_network.py](ui/tab_network.py)で`st.session_state['project']['name']`からプロジェクト名を取得し「共起ネットワーク — {プロジェクト名}」の形で渡す。`network_template.html`側で、タイトル（左上、背景付き）と凡例（右上、背景付き——単語ノードの色分け凡例〈属性への接続数で色分けしている場合は少/多の2色、していない場合は単色〉、属性値ノードの凡例〈属性マッピング有効時のみ〉、エッジの太さ＝共起の強さの凡例）を、`zoomLayer`ではなく`svg`直下に追加——パン・ズーム操作で一緒に動かない固定オーバーレイとし、他の要素より後に追加することで常に最前面に表示されるようにした。
- **検証**: 実データ相当の偏りを再現した合成データ（40語のハブA・5語のハブB・3語のハブC、バブルサイズ倍率2.0）で一時的な検証用Streamlitハーネス（検証後削除）を`streamlit run`で起動し、`iframe.contentDocument`を`javascript_tool`経由でクエリして検証: タイトルに「共起ネットワーク — テストプロジェクト」、凡例に「凡例」の文字列が実際に描画されていることを確認。全単語ノード（50個）の実際の視覚的な外接矩形（中心座標＋半径から算出、SVGの入れ子`<g transform="translate(...)">`を全て親までたどって絶対座標を算出）がキャンバス範囲内に収まっていることを確認——最初の簡易チェック（直近の祖先`g[transform]`のみを見る実装）では凡例の入れ子グループの相対座標を誤って絶対座標と解釈する不具合があり誤検知したため、祖先を`<svg>`まで辿って変換を合成する実装に修正した上で再検証し、0件のはみ出しを確認。`read_console_messages`でJSエラー無し。クリーンな`streamlit run`起動後、本体アプリもエラー無く起動することを確認。

- 2026-08-14（続き）: 上記の改善を試したユーザーから「改善されていない」と再度フィードバック。目標にしたいKH Coderの実例3件（Jaccard/cosine係数の違い、3属性/4属性の違い）のHTMLファイル（`jaccard_3crass.html`, `jaccard_4crass.html`, `cocine_3crass.html`）を提供され、「係数からルールを作って１属性とだけエッジをつなぐ単語、２属性の中間になるようエッジをつなぐ単語……のようにエッジが選ばれて作成されていると思われる」との分析を受けた。

**KH Coderの実HTML出力を解析し、根本的な設計ミスを発見**: 3件ともR `networkD3`パッケージの`forceNetwork`ウィジェット形式（`<script type="application/json" data-for="htmlwidget-net">`に埋め込まれたJSON）で、Pythonで実際にパースして構造を調べたところ——
  - **単語×単語エッジが1本も存在しない**（3ファイルとも`word-word: 0`、全エッジが`word-attr`）。属性マッピング時のKH Coderの共起ネットワークは、単語同士の共起を一切考慮しない**純粋な単語×属性値の二部グラフ**だった。
  - **エッジ総数は3ファイルとも60本**（ノード数36〜49、属性ハブ数3〜4と条件が異なるのに一致）——「上位60」のような固定本数のグローバルな足切りをしていると判明。
  - **単語ごとの接続属性数は自然にばらつく**（例: 4属性の例では1属性のみ19語、2属性2語、3属性7語、4属性〈全属性〉4語）——単語ごとに1本などに制限する仕組みは無く、係数の高いエッジがグローバルな上位60本の足切りを超えた分だけ複数のハブへのエッジが残る、という単純なルールで説明がついた。
  - これにより、前回・前々回に実装していた「各単語は最も強い属性値ハブ1本だけを残す」（`max_attr_edges_per_word`）という設計、およびそれに伴うハブグループのメンバー数比例拡縮・セントロイド平行移動ロジックが、そもそもの前提から誤りだったと判明した。単語が複数ハブの「中間」に位置して見えていたのは特殊なレイアウト処理の結果ではなく、単純に複数の実エッジが残っていて力学的に引っ張られていただけだった。

**単語×属性値の純粋な二部グラフへ刷新**:
- `core/network.py`の`build_graph`: `max_attr_edges_per_word`（単語ごとの上位K本）を削除し、`max_attr_edges`（語×属性値エッジ全体からグローバルに上位N本、`max_word_edges`と同じロジック、既定60——KH Coderの実例に合わせた）に置き換え。単語ごとのグルーピング処理は完全に削除。
- `ui/tab_network.py`: 属性マッピング有効時は単語×単語エッジを`build_graph`に一切渡さない（空リスト）。「単語ごとの最大属性エッジ数」スライダーを削除し「最大エッジ数（単語×属性値）」（既定60）に置き換え。「最大エッジ数（語×語）」のヘルプに属性マッピング時は使用されない旨を追記。
- `core/network_viz.py`の`compute_layout`: 属性値ハブがある場合、単語×属性値の全エッジを含むグラフに対し、igraphの`layout_fruchterman_reingold`の`minx`/`maxx`/`miny`/`maxy`引数でハブノードの座標を厳密に固定（`min=max`で実質ピン留め、実機検証で機能を確認済み）した状態で一回だけFRレイアウトを計算する方式に刷新——単語は実際に持つエッジ（1本〜複数本）に基づいて自然にハブへ引き寄せられ、複数ハブに強く繋がる語は自然にその中間に位置する。旧来の「単語×単語サブグラフのFR→ハブごとにグルーピング→セントロイド平行移動→メンバー数比例拡縮」という多段階処理は全て削除し、コード量も大幅に削減された。
  - **実装中に発見した追加の技術的知見**: igraphのFruchterman-Reingoldは内部的にほぼ単位スケール（原点付近、半径1前後）を前提に力の強さが調整されており、ピクセル単位（数百）の座標をそのまま`minx`/`maxx`等に使うと反発力に対して引力（Jaccard係数がそのまま強さになる）が相対的に弱すぎて、単語ノードが実際に繋がっているハブとは無関係な位置に飛んでしまうことを実機検証で発見。対策として、レイアウト計算自体は単位スケール（ハブ半径=1.0、単語の可動範囲=±1.3）で行い、計算後にピクセル座標へ変換する方式に変更。また、Jaccard係数（0〜1の小さい値）をそのまま`weights`に渡すと同じハブに多数の語が集中するデータ（実データでよくある偏り）で反発力に負けて散らばってしまうことも発見し、`_WEIGHT_SCALE=300`で引力を引き上げることで、あるハブに接続する単語がそのハブの近くに適度にまとまりつつ極端に潰れもしない、実用的なバランスになることを確認（30倍では効果不十分、100〜500倍の範囲で検証し300を採用）。`_layout_word_subgraph`（属性マッピング無し用パス、既に「ほぼ完璧」と評価済み）はseed付きの元々の実装のまま変更していない。
- **検証**: `build_graph`の新しい`max_attr_edges`をスタンドアロンスクリプトで検証（グローバル上位N本が単語ごとではなく全体から選ばれること、同じ単語が複数属性へのエッジをその都度自然に持ちうることを確認）。`compute_layout`もスタンドアロンスクリプトで検証: 小規模な合成データ（4語・3ハブ、1本のみ/2本の語が混在）でハブ位置が計算式に厳密に一致すること、複数ハブに繋がる語がその両方に対し無関係なハブより明確に近いことを確認。KH Coderの実例規模に近い合成データ（単語45・ハブ3、うち大多数が1ハブのみ・一部が2〜3ハブに接続——実際に`{1本:35語, 2本:9語, 3本:1語}`という実例と似た自然な分布になることを確認）を一時的な検証用Streamlitハーネス（検証後削除）で実際に`streamlit run`起動し、`iframe.contentDocument`を`javascript_tool`経由でクエリして、単語×単語エッジが0本であること（`<line>`本数が期待値と完全一致）、多数の語が集中するハブでも各語が現実的な距離（数十〜百数十px、以前は最大500px超えで実質無関係な位置だった）に収まることを実測して確認。`read_console_messages`でJSエラー無し（サーバー再起動直後の接続エラーは無関係なノイズと判別）。クリーンな`streamlit run`起動後、本体アプリもエラー無く起動することを確認。

- 2026-08-14（続き）: 二部グラフ刷新をユーザーが自身のブラウザで実際にテストし「近づいてますね」と前進を確認。追加の微調整要望4点: ①全体が円の中に収まるように、②エッジは線の太さを細く統一し色の濃淡（不透明度）だけで共起の強弱を表現、③ノードの輪郭線も細く、④左上タイトルはプロジェクト名を2段目にレギュラーウェイトで。

- **全体を円形の構図に**: [core/network_viz.py](core/network_viz.py)の`compute_layout`——単語ノードの拘束が矩形（minx/maxx/miny/maxy）だけだと、四隅方向に広がった語がある場合に全体が四角っぽく見えることがあったため、ピクセル座標へ変換した後に中心からの距離でクランプする円形の境界処理を追加（半径は`_UNIT_WORD_BOUND × hub_radius_px`——既存のFR拘束と同じ論理半径を流用）。合成データで、全単語ノードの中心からの距離が円の半径を超えないことを確認。
- **エッジ・輪郭線の細線化**: [ui/network_template.html](ui/network_template.html)——エッジの`stroke-width`を係数に応じたスケール（1〜5px）から固定の細い値（1.2px）に変更し、共起の強弱は`stroke-opacity`（不透明度、既存のまま）だけで表現するようにした。凡例のエッジ強弱サンプルも幅ではなく不透明度の違いで示すよう修正。ノード（`circle`/`rect`）と凡例スウォッチの`stroke-width`も2px→1pxに統一。
- **タイトルの2段組**: [ui/network_component.py](ui/network_component.py)の`build_network_html`に`subtitle`引数を追加（プロジェクト名を渡す）。`network_template.html`側で、1段目「共起ネットワーク」（太字16px）はそのまま、2段目にプロジェクト名をレギュラーウェイト12pxで追加表示し、背景の角丸矩形は両方の行を覆うようサイズを動的に計算するよう変更。`ui/tab_network.py`は`title='共起ネットワーク'`固定＋`subtitle=project_name`を渡す形に整理（以前は`f'共起ネットワーク — {project_name}'`という1行連結だった）。
- **検証**: `compute_layout`の円形クランプをスタンドアロンスクリプトで検証（45語・3ハブの合成データで、全単語の中心距離が計算上の境界半径と厳密に一致する上限に収まることを確認）。一時的な検証用Streamlitハーネス（検証後削除）で実際に`streamlit run`起動し、`iframe.contentDocument`を`javascript_tool`経由でクエリして、全エッジの`stroke-width`が単一の値（1.2）に統一されていること・`stroke-opacity`は係数に応じて多様な値（57種）を保っていること、ノード`stroke-width`が1であること、タイトルグループ内に「共起ネットワーク」（16px・bold）と実際のプロジェクト名（12px・normal）の2行が期待通りの位置に描画されていることを確認（初回のクエリはCSSセレクタが緩くエッジラベルのテキストを誤って拾ってしまったため、タイトル専用のg要素を構造的に特定するよう修正して再検証）。`read_console_messages`でJSエラー無し。クリーンな`streamlit run`起動後、本体アプリもエラー無く起動することを確認。

- 2026-08-14（続き）: ユーザーが自身のブラウザで3属性・4属性の両方のケースを実際にテストし「近づいてますね」「これもかなりいい感じです」と好反応。追加の微調整要望2点: ①参考にしたKH Coderの見た目では属性を中心にした小さな円に語がまとまって見える（前回実装した「キャンバス全体を1つの円にまとめる」処理では不十分）、②水色の塗りの透明度をもう少し下げてほしい。

- **各ハブを中心にした小さな円へのクランプ**: [core/network_viz.py](core/network_viz.py)の`compute_layout`——前回実装した「キャンバス中心からの距離でクランプ」を撤廃し、代わりに各単語について最も近い属性値ハブを特定し、そのハブを中心にした半径`_UNIT_HUB_LOCAL_RADIUS（0.65）× hub_radius_px`の小さな円の中に収まるようクランプする方式に変更。45語・3ハブの合成データで、全単語の最寄りハブからの距離が計算上の境界半径（159px）を厳密に超えないことを確認。
- **塗りの透明度をさらに下げる**: [ui/network_template.html](ui/network_template.html)——単語ノード・属性値ノードの塗り（`FILL_COLOR`使用箇所、凡例スウォッチ含む計4箇所）の`fill-opacity`を0.45→0.3に変更。タイトル・凡例パネルの背景（`BG`使用箇所）はそのまま維持。
- **検証**: `compute_layout`の新しいハブ中心クランプをスタンドアロンスクリプトで検証（上記の通り）。一時的な検証用Streamlitハーネス（検証後削除）で`streamlit run`起動し、`iframe.contentDocument`を`javascript_tool`経由でクエリして、ノード・属性値ノードの`fill-opacity`が0.3になっていることを確認。`read_console_messages`でJSエラー無し。本体アプリ（ユーザーが自身のブラウザで実際に開いていたため、今回はこちらのサンドボックスから再起動せず、Streamlit標準のファイル変更検知バナー〈Rerun〉をユーザー自身に使ってもらう案内に留めた）。

- 2026-08-14（続き）: ユーザーが3・4・6属性の実データでR9（ハブ中心クランプ＋透明度低下）を再テスト。バブルの密集感自体は「バブルサイズをやや大きめにしているため」と自己診断（バブルサイズスライダーは手動調整可能なので対応不要と判断）した上で、別の構造的な問題を指摘: 属性の並び順（頻度降順）と固定角度（`-90°`始まり）の組み合わせにより最多属性（例:「一般教員（校長・教頭以外）」）が常にキャンバス最上部に配置され、その語クラスターが上端で見切れてハブ直下に横並びに詰まって見える。また「正三角形」のハブ配置全体を少し小さく、かつ全体をもう少し下寄りにしてほしい、中央から下方向の空白が間延びして見える、との要望。

- **ハブ配置の半径縮小＋中心の下方シフト**: [core/network_viz.py](core/network_viz.py)の`compute_layout`——ハブ配置円の半径係数を`min(width,height)×0.35`から`×0.23`（`_HUB_RADIUS_FACTOR`）に縮小し、配置円の中心`cy`を`height/2`からさらに`height×0.035`（`_HUB_CENTER_Y_SHIFT`）下へずらすよう変更。この2つの定数は独立に決めたのではなく、「ハブ位置±ハブローカル半径（`_UNIT_HUB_LOCAL_RADIUS×hub_radius_px`）」がキャンバス上下端どちらのmargin（40px）も侵さない、という制約から逆算した——属性数が偶数（4・6属性など）の場合は等角配置により最下部にもハブが来るため、下方向のシフトを大きくしすぎると今度は最下部のハブが同じ問題を起こしてしまう（3/4/6属性の合成データで実際に検証し、旧数値のまま下方シフトのみ加えると4属性ケースの最下部ハブがはみ出すことを発見→半径をさらに絞って再検証し解消）。
- **検証**: 3・4・6属性それぞれの合成データ（各ハブに15語ずつ接続）でスタンドアロンスクリプト検証——`compute_layout`が返す各属性値ハブの座標について、ハブ中心±ハブローカル半径の外接矩形がキャンバスの安全マージン（40px）を全方向で下回らない（＝はみ出さない）ことをアサート。旧定数（半径0.28・シフト0.05）では4属性ケースの最下部ハブが下端で約48pxはみ出すことを実際に検出→新定数（半径0.23・シフト0.035）で3・4・6属性いずれも全ハブが余裕（最小で上下左右とも約5〜70px）を持って収まることを確認。本体アプリ（`__pycache__`削除→再起動、ユーザーの既存セッションの`_network_layout_cache`はレイアウト定数変更を検知できない設計〈グラフ構造・サイズのみをキャッシュキーに含む〉ため、変更を反映させるにはサーバー再起動が必要と判断し実施）を再起動し、`read_console_messages`相当の`preview_logs`でエラー無いことを確認。

- 2026-08-14（続き）: ユーザーがR10（ハブ配置の縮小・下方シフト）を自身のブラウザで実際にテストし「良くなりました」と好反応。3・4・6属性いずれのスクリーンショットも上端の見切れ・詰まりが解消されていることを確認。追加の微調整要望2点: ①右上の凡例パネルを角丸でない矩形に、②「凡例」という見出しテキストは不要。

- **凡例パネルの角丸撤去・見出し削除**: [ui/network_template.html](ui/network_template.html)——凡例背景`<rect>`から`rx`属性を削除（角丸→直角）。「凡例」の`<text>`見出し要素を削除し、それに伴い見出し分確保していた縦方向の余白（`legendHeight`計算の`+ 20`、`legendItemsGroup`の`translate`オフセットの`+ 22`）も撤去して詰めた。凡例パネル自体は`ui/network_component.py`の`build_network_html`が呼び出しごとにテンプレートファイルを`read_text`で読み直す設計（キャッシュ無し）のため、レイアウト計算（`_network_layout_cache`）とは無関係——通常のStreamlitファイル変更検知（Rerun）だけで反映される。
- **検証**: 合成データ（単語1・属性値1）で一時的な検証用Streamlitハーネス（検証後削除）を`streamlit run`で起動し、`iframe.contentDocument`を`javascript_tool`経由でクエリして、凡例`<rect>`に`rx`属性が存在しない（`getAttribute('rx')`が`null`）こと、凡例グループ内の全`<text>`が項目ラベルのみ（「凡例」の文字列が含まれない）ことを確認。

- 2026-08-14（続き）: 共起ネットワークの一連の調整をユーザーが確認し「確認しました」「一旦ここまで」と区切り。続けて「ここで使ってるフォントを教えてください」と質問を受け、[ui/network_template.html](ui/network_template.html)のSVG用CSSフォントスタック（`"Yu Gothic", "Hiragino Kaku Gothic ProN", Meiryo, sans-serif`）を回答したところ、「使用フォントを統一したい」と要望。

**フォントスタックの統一**: 調査の結果、ワードクラウド・クラスター分析（matplotlib）は[core/fonts.py](core/fonts.py)の`resolve_japanese_font`が実行時に`C:\Windows\Fonts\`から優先順（NotoSansJP-VF.ttf → meiryo.ttc → msgothic.ttc）で解決したフォントファイルを使っており、このマシンでは`NotoSansJP-VF.ttf`が存在するため実際には**Noto Sans JP**が使われている（matplotlibの`FontProperties(fname=...).get_name()`で実際の登録名が`"Noto Sans JP"`であることを実機で確認）。一方、共起ネットワークのSVG（ブラウザ側CSS）は全く別の優先順（Yu Gothic優先）だったため、このマシンではNoto Sans JPとYu Gothicという異なる書体が同時に使われていた——これがユーザーの質問の背景。[ui/network_template.html](ui/network_template.html)のSVG本体・ツールチップ、計2箇所の`font-family`を、`core/fonts.py`の優先順に合わせて`"Noto Sans JP", Meiryo, "MS Gothic", "Hiragino Kaku Gothic ProN", sans-serif`に変更（Noto Sans JPが未インストールの環境向けにMeiryo/MS Gothicの順でフォールバック、Hiragino Kaku Gothic ProNはMac向けの保険として末尾に残した）。埋め込みフォント化（SVGへのbase64 @font-face埋め込み）はせず、ブラウザのシステムフォント解決に委ねる方式は維持——このアプリはユーザー自身のマシンでのみ動くローカルツールであり、`core/fonts.py`側も同様にシステムフォントを都度解決する設計のため、一貫性のある選択と判断。
- **検証**: 合成データ（単語1・属性値1）で一時的な検証用Streamlitハーネス（検証後削除）を`streamlit run`で起動し、`iframe.contentDocument`を`javascript_tool`経由で`getComputedStyle`をクエリして、SVG本体・ノードラベル双方の計算済み`font-family`が新しいスタック文字列と完全一致することを確認。

- 2026-08-14（続き）: 共起ネットワークの調整が一区切りついたところで、ユーザーから「本日中にワードクラウドまでできないでしょうか？これは比較的軽いと思います」と依頼。まず改善内容を確認したところ、「d3.min.jsをローカルで起動しブラウザ上に表示」「フォントの太さと密度に程よい間があり息苦しさがなく美しい」「マスクもゆるくふわふわとしていて雲っぽい」という参考イメージを説明され、共起ネットワークと同様にD3.jsベースへ作り直す方向で合意しかけた。しかしユーザーが実際の参考画像（PowerPointスライド3枚、教員アンケートの頻出単語ワードクラウド）を提示したところ、輪郭マスクの無い自然な楕円状のブロブ・強い大小差・多色・横書きのみという特徴が、Python `wordcloud`ライブラリのマスク無し時の標準的な見た目そのものだったため、D3化を提案せず**既存のPythonエンジンのパラメータ調整**で近づける方針に転換した（D3化は共起ネットワークではパン・ズームやホバー係数表示といったインタラクティブ性・igraphによる座標精密制御という実利があったが、ワードクラウードは最終的に1枚の静止画のため、書き直す実利が乏しいと判断）。ユーザーも同意。

**参考画像に近づけるパラメータ調整**: [core/wordcloud_gen.py](core/wordcloud_gen.py)の`generate_wordcloud`——`relative_scaling`を既定値0.5相当から`0.6`に、`margin`を既定2pxから`4px`に変更し、大小のメリハリを強め語同士の間隔を広げて「息苦しさ」を軽減。マスク形状は既存の「なし」がUIの既定選択のままで参考画像の輪郭無しブロブ形状と一致するため変更不要。新たに`make_multicolor_color_func`を追加——品詞やカテゴリに関係なく、既存の10色パレット（`CHART_COLORS`、After Coderと共有）から語ごとにランダムに配色する（wordcloudライブラリが`color_func`に渡す`random_state`——`random.Random`インスタンス、無ければ標準`random`モジュールにフォールバック——の`randint`で選択）。[ui/tab_wordcloud.py](ui/tab_wordcloud.py)の「色分けモード」に「多色ランダム」を追加（既存の「品詞別4色」「モノクロ3段階ネガポジ」との3択に拡張）。
- **検証**: 参考画像の頻出語分布を模した合成データ（大小差の大きい約80語）でスタンドアロンスクリプトを実行し、生成されたPNGを`Read`ツールで直接目視確認——輪郭マスク無しの自然な楕円ブロブ形状、強い大小差、10色パレットによる賑やかな多色配色、横書きのみ、語同士に窮屈でない間隔、という参考画像の特徴と近い結果になっていることを確認。`ui/tab_wordcloud.py`・`core/wordcloud_gen.py`とも`python -c`によるインポート確認でエラー無し。ワードクラウド生成はStreamlit側で`st.session_state`キャッシュを使っていないため（共起ネットワークのレイアウトキャッシュとは異なる）、本体アプリは再起動不要——通常のファイル変更検知（Rerun）で反映される。

- 2026-08-14（続き）: ユーザーが実データ（教員アンケート、属性フィルタ適用時）で試用し、「最大フォントが大きすぎる　太すぎと細すぎ」と指摘。実データでは特定の最頻語（「職員」「管理職」など）が他語より突出して多く出現するため、`relative_scaling=0.6`（頻度差をそのまま強く反映）が最大フォントを過大にし、結果として最大と最小のコントラストが極端になっていたと判断。
  - **調査メモ**: ユーザーのスクリーンショットには一部の語（「働き方改革」「人間関係」「パワハラ」等）が黒枠の白い矩形で囲まれて表示される箇所も見えたため、太字フォントのキャッシュ破損（`core/fonts.py`の`_BOLD_CACHE_PATH`、fontToolsによる可変フォントのウェイト固定インスタンス化結果を一時ファイルにキャッシュする仕組み）を疑い、実際のキャッシュファイル（`%TEMP%\word_counter_ja_bold_600.ttf`）を使い、同じ語・bold True/False両方でスタンドアロン検証したが再現せず、クリーンに描画された。原因不明のため深追いはせず、フォントサイズの是正のみを実施——次回、矩形の再現有無・発生タイミング（フィルタ変更直後かどうか等）を確認する。
  - **最大フォントサイズの是正**: [core/wordcloud_gen.py](core/wordcloud_gen.py)の`generate_wordcloud`——`relative_scaling`を0.6→`0.4`（既定0.5よりやや弱め、頻度差の反映を抑えて極端な大小差を防ぐ）に変更。新たに`max_font_size=int(height * 0.32)`を明示的に指定（従来は`None`で上位2語から自動算出——頻度差が極端な実データだと大きくなりすぎていた）。
  - **検証**: 実データを模した、最頻語が突出して多い合成データ（「職員」90回 vs 大半が5〜30回程度）でスタンドアロンスクリプトを実行し、生成PNGを目視確認——最大フォントが以前ほど突出せず、全体のサイズ分布が参考画像に近い落ち着いたバランスになっていることを確認。

- 2026-08-14（続き）: ユーザーが調整後を実データで再テストし、「フォントに違和感が。見本と同じフォントですか？」「最大文字サイズや文字サイズ最大から最小へのカーブが選べると良い」とフィードバック。
  - **フォントについて**: 調査の結果、ワードクラウドは既に`core/fonts.py`経由でNoto Sans JP（今回の「フォント統一」で共起ネットワーク・クラスター分析と揃えた同じフォント）を使っており、ユーザーが提示したPowerPoint参考画像は別ツール（おそらくOffice標準のメイリオ/游ゴシック）で作られたものと思われ、字形の設計思想の違いが「違和感」の正体だろうと回答。フォントファミリーを変更するか確認したところ、**「今のNoto Sans JPのままでよい」**との回答——変更なし。
  - **最大文字サイズ・メリハリカーブのスライダー化**: 前回ハードコードした`max_font_size`（`height*0.32`固定）・`relative_scaling`（0.4固定）を、[core/wordcloud_gen.py](core/wordcloud_gen.py)の`generate_wordcloud`の引数`max_font_size_ratio`（既定0.32）・`relative_scaling`（既定0.4）として外出しし、[ui/tab_wordcloud.py](ui/tab_wordcloud.py)に「最大文字サイズ」「文字サイズのメリハリ」の2スライダーを追加してユーザーが調整できるようにした（ライブプレビュー・高解像度ダウンロード両方の`generate_wordcloud`呼び出しに配線）。
  - **検証**: `generate_wordcloud(freq, max_font_size_ratio=0.5, relative_scaling=0.8)`をスタンドアロンで呼び出し、生成された`WordCloud`オブジェクトの`.max_font_size`・`.relative_scaling`属性が指定値と一致することを確認。`ui/tab_wordcloud.py`のインポート確認でエラー無し。

- 2026-08-14（続き）: ユーザーから「本日はここまでにしましょう。どこか押しが強すぎる密度感なんです」とのクローズコメント。具体的な指示ではなく、全体的な視覚密度（太字＋大小差＋余白のバランス）がまだ「押しが強い」＝主張しすぎる印象という感覚的なフィードバック。次回、この「押しの強さ」を構成する要素（太字のON/OFF・`margin`・`relative_scaling`・`max_font_size_ratio`の組み合わせ）を実データで見ながら一緒に調整する必要がある——本セッションでは未対応（ユーザーの終了宣言のため着手せず）。

- （セッション再開）ユーザーからクロス集計に入る前の質問として「表記ゆれ統合は内部的に何をしているか、元データの表記を書き換えるのか」と質問を受け、[core/tokenizer.py](core/tokenizer.py)の`apply_variant_map`が各トークンの`normalized`フィールドのみを差し替え、`surface`（元の表記）は一切変更しないこと、トークナイズ自体は1回だけキャッシュされ`variant_map`は毎回のレンダリングで後から適用される後処理であることを回答（詳細はコード参照）。

**LLM表記ゆれ統合UIの刷新（個別チェックボックス化）**: ユーザーから「確定時に個別の提案にチェックボックスを付け、最下部の「チェックした項目を「同義語とする」／「同義語としない」」の2ボタンで登録する」という要望。[ui/tab_cleaning.py](ui/tab_cleaning.py)の`_render_variant_grouping`——従来の「✅ 確定する（全件一括）／❌ キャンセル（全件一括）」の2ボタンを廃止し、提案グループごとに`st.checkbox`（既定でチェック済み）を表示、最下部の2ボタン（「チェックした項目を「同義語とする」」「チェックした項目を「同義語としない」」）はチェックされた項目のみを処理する方式に変更。「とする」はチェック済みグループを`variant_map`へマージしつつ`pending_variant_groups`から除去、「としない」はチェック済みグループを`variant_map`に加えず`pending_variant_groups`から除去するだけ——チェックを外した項目は次回の操作までpendingに残るため、複数回に分けて選別できる。

- （続き）: ユーザーから「表記揺れ辞書は使っていないのか」と質問を受け、リポジトリ内・メモリいずれにも該当ファイルの記録が無いことを回答したところ、ユーザーの勘違いと判明（いったんセッション中断）。再開後、KH Coder付属の`TANSI_v110`辞書ファイル（`C:\Users\ryu\Documents\C_Storage\■MJ吉澤研究\【重要】■■テキストマイニング\【重要】■■KH_Coder\KH Corder\TANSI_v110\TANSI_v110.txt`）を提示され、利用可能か確認された。

**TANSI表記ゆれ辞書の調査**: タブ区切り6列・約30万行・37MBのファイルと判明。列は「表記／読み／発音（長音正規化済み）／品詞／活用形／同一グループの表記ゆれ一覧（`/`区切り、自分自身含む）」——KH Coder付属の一般的な日本語表記ゆれ辞書で、「ええと/えーと」のようなフィラーから「問い合わせ/問合せ/といあわせ」のような業務寄りの語まで広くカバーする。ユーザーに統合方針を確認し、**「辞書とLLMを併用する（推奨）」**——辞書ベース検出（無料・即時、`発音`列の一致でグルーピング）をまず行い、辞書でカバーできなかった候補語のみLLMに提案させる方針で合意。

**辞書ベース表記ゆれ検出の実装**: [core/variant_grouping.py](core/variant_grouping.py)に追加。
- `_TANSI_DICT_PATH`: ユーザーのマシン固有の外部パスを直接ハードコード参照する（[core/fonts.py](core/fonts.py)のWindows標準フォント解決と同じ、固定パス＋ファイルが無い環境ではグレースフルデグレードのパターン。37MBの汎用辞書はプロジェクトに同梱せずgit管理対象外のまま）。
- `_load_tansi_reading_map()`: ファイルを`{表記: 発音}`の辞書にパースして`functools.lru_cache(maxsize=1)`でプロセス内キャッシュ（30万行の逐次パースは初回のみ約1.4秒、以降は再利用）。ファイルが見つからない環境では`None`を返す。
- `propose_dictionary_variant_groups(candidate_words)`: 候補語リスト（頻度降順を想定）を`発音`が一致するグループにまとめる。各グループの代表語（canonical）はリスト内で最初に登場した語（＝最頻語）とし、残りをmembersとする。LLM提案と同じ`{canonical, members, reason}`の形に加え`'source': '辞書'`を付与し、LLM提案側にも`'source': 'LLM'`を付与して両者を区別できるようにした。

**`ui/tab_cleaning.py`の統合フロー刷新**: 「候補を提案」ボタン押下時、まず`propose_dictionary_variant_groups`を全候補語（頻度上位80語）に対して実行し、辞書でカバーされた語（各グループのcanonical+members）を`covered`集合として除外した残りだけをLLMの候補語として渡すよう変更。APIキー未設定でも辞書ベースの提案だけは動作するため、ボタンの`disabled=not api_key`制約を撤廃し、APIキー未設定時は「辞書ベースの提案のみです」という案内キャプションに変更。提案一覧の各行に`` `辞書` ``/`` `LLM` ``のバッジ（Markdownのコードスパン）を出典として表示。
- **検証**: `_load_tansi_reading_map`を実ファイルで読み込み（24.7万エントリ、初回約1.4秒）、`propose_dictionary_variant_groups(['まぁ','仕事','会議','まあ','まあー','問い合わせ','問合せ','教員','といあわせ'])`をスタンドアロンスクリプトで実行し、期待通り「まぁ←まあ、まあー」「問い合わせ←問合せ、といあわせ」の2グループが読み一致で正しく形成され、無関係語（仕事・会議・教員）が混入しないことをJSON出力経由で確認（コンソール直接出力はWindows環境の文字コードの都合で文字化けするため、ファイル書き出し→`Read`ツールでの確認に切り替えた）。一時的な検証用Streamlitハーネス（`ui/tab_cleaning.py`の`render()`を合成トークンで直接呼び出す、検証後削除）を実際に`streamlit run`起動し、`javascript_tool`でボタンクリックを含む実際のブラウザ操作を行い、①「候補を提案」クリックで辞書ベースの提案（`` `辞書` 問い合わせ ← 問合せ``、理由「辞書（読み「トイアワセ」が一致）」）が正しく表示されること、②「チェックした項目を「同義語とする」」クリック後、提案が消えて「適用中の統合ルール（1件）」に切り替わることを実際のDOM操作で確認した（テストデータの一部語が意図せず品詞カテゴリ「名詞B」〈デフォルトの対象カテゴリ外〉に分類され候補から漏れる場面があったが、これは`core/pos_rules.py`の既存の分類ロジックによるテストデータ側の想定漏れであり、今回の実装のバグではないことを`map_pos`の個別確認で切り分け済み）。`read_console_messages`相当のログ確認は本ハーネスがStreamlit標準機能のみ使用のため省略。

- （続き）: 表記ゆれ統合の質疑が一段落したところで、ユーザーから「クロス集計の前にコーディング集計のイメージを話したい」と切り出され、「年配」というコードに「年配／ベテラン／年長／経歴の長い／年寄り／高齢／経験豊か」のような、表記が全く異なる語句を意味的に束ね、そのコードについて出現率・共起分析・クラスター分析を行いたいという要望を受けた。既存の[core/codebook.py](core/codebook.py)（`parse_codebook`/`apply_codebook`）でグルーピング自体は実現できることを確認した上で、AskUserQuestionで2点を確認: ①共起分析はコードを通常の語と**混ぜて**分析（関係性理解が目的）、属性クロス集計はコード同士・属性のみで比較（量的比較が目的）——分析の種類ごとに使い分ける、②新規タブに追加するのではなく既存の「クロス集計」タブを「コーディング集計」に改名し、出現率・共起分析・クラスター分析を全て統合する。EnterPlanModeで2段階のExploreサブエージェント（実装コード調査）＋Planサブエージェント（設計レビュー）を経てから実装した。

**設計レビューで発見された重大なノード衝突バグ（実装前に対策込みで設計）**: ユーザー自身の例（コード「年配」のトリガーに「年配」という語自体を含む）は、コードを共起ネットワーク・クラスター分析に混ぜる際に**素の語彙とコード名が文字列として衝突する**典型例だった——`build_cooccurrence_edges`は語プールとattr（コード）プールを同じ`freq`/`node_types`辞書にキーとして統合するため、衝突すると片方が上書きされ、語とコードが同一ノードとして混ざってしまう。対策として、コードを語彙分析パイプラインに混ぜる直前に常に一意な記号を付ける方式にした。当初「［コード］」という接尾辞を検討したが、ユーザーから「ワードクラウドでの使用を考えた場合コードの表記は大きすぎる、一般利用率が低い小さな記号を」との要望を受け、通常の日本語文章にまず登場しないダガー記号（**†**）1文字に変更した（例:「年配」→「年配†」）。

**実装**:
- [core/codebook.py](core/codebook.py)に3関数を追加: `label_codes(code_doc_sets)`（コード名に`†`を付与、衝突防止）、`code_occurrence_rates(code_doc_sets, n_docs)`（出現率＝該当文書数/総文書数）、`code_doc_matrix(code_doc_sets, n_docs)`（`core/clustering.py`の`build_word_doc_matrix`と同じ形の`(labels, bool ndarray)`に変換、`label_codes`を内部で適用）。
- [ui/network_component.py](ui/network_component.py)の`build_network_html`に4つの表示文言引数を追加（`attr_kind_label`＝既定「属性値」、`attr_degree_label`＝既定「接続属性数」、`attr_connection_low_label`／`attr_connection_high_label`＝既定「単語（属性への接続少/多）」）。既定値は現状と同一のため[ui/tab_network.py](ui/tab_network.py)の呼び出し側は無変更で動作する。[ui/network_template.html](ui/network_template.html)側は、ツールチップの種別表示・接続数ラベル・凡例3項目（計4箇所）でハードコードしていた「属性値」「接続属性数」等の文言を、対応するプレースホルダ（`__WC_ATTR_KIND_LABEL_JSON__`等）から読んだJS定数に置き換えた——`core/network.py`自体はattrノードが実際に何を表すか関知しない汎用設計だが、D3テンプレート側は属性専用の文言をハードコードしていたため、この差し替えが必要だった（Planエージェントのレビューで発見）。
- [core/project.py](core/project.py)の`build_project`に`codebook: str = ''`をオプション引数として追加（`stopwords`と全く同じパターン、`_REQUIRED_KEYS`には含めず旧形式プロジェクトファイルとの互換性を保つ）。[ui/sidebar.py](ui/sidebar.py)に`variant_map`と同じパターンで3箇所（`_PROJECT_RESET_KEYS`、`_restore_project_state`、`_sync_or_create_project`）を配線し、コードブックがプロジェクト保存・復元の対象になるようにした。
- [ui/tab_crosstab.py](ui/tab_crosstab.py)を全面刷新（ファイル名・関数名はリネームせず、ユーザー向け表示文言のみ「コーディング集計」に統一）: `render()`に`included_categories`/`stopwords`を追加。①**出現率**（新規、属性の有無に関わらず常時表示、`.1f%%`書式）、②**属性クロス集計**（既存機能をそのまま流用、属性が無い場合はこのセクションのみ案内メッセージに切り替え——以前は属性が無いとタブ全体が使えなかった制約を撤廃）、③**コードの共起分析**（新規、`label_codes`したコードを`build_cooccurrence_edges`の`attr_doc_sets`に渡し、`word_word_edges=[]`の純粋二部グラフとして描画。独自レイアウトキャッシュキー`_coding_network_layout_cache`で共起ネットワークマップタブの`_network_layout_cache`と非衝突。`build_network_html`に`attr_kind_label='コード'`等を渡し文言を差し替え）、④**コードのクラスター分析**（新規、`code_doc_matrix`と既存`build_word_doc_matrix`の行列を`np.vstack`で結合、既定「語も含める」ON。該当文書0件のコードは除外しキャプションで件数表示、対象2件未満は`tab_clustering.py`と同じ警告パターンでガード）。全ての新規ウィジェットに明示的な一意`key=`（`coding_net_*`/`coding_cluster_*`）を付与——`st.tabs`は非アクティブタブも同一スクリプト実行内で描画するため、既存の共起ネットワーク／クラスター分析タブと同じラベルのウィジェットがあると`StreamlitDuplicateElementId`になるおそれがあったため（Planエージェントのレビューで発見）。
- [app.py](app.py)のタブ名を「クロス集計」→「コーディング集計」に変更、`tab_crosstab.render(...)`呼び出しに`included_categories`/`stopwords`を追加。
- **検証**: `core/codebook.py`の新規3関数をスタンドアロンスクリプトで検証（正常系・n_docs=0のエッジケース）。**ユーザー自身の衝突シナリオ**（コード「年配」のトリガーに「年配」自体を含み、かつ通常の語彙プールにも独立して「年配」というトークンが頻出する合成データ）を用意し、`apply_codebook`→`label_codes`→`build_cooccurrence_edges`→`build_graph`まで通しで実行し、素の語「年配」（`node_type='word'`, freq=3）とコード「年配†」（`node_type='attr'`）が上書きされず別ノードとして共存することをアサート——全て成功。さらに`app.py`と同じ`st.tabs`構造（共起ネットワークマップ・クラスター分析・コーディング集計を同一スクリプト内に描画）を再現した一時的な検証用Streamlitハーネス（検証後削除）を`streamlit run`起動し、`javascript_tool`で実際にタブ切り替え・チェックボックス操作を行い: 出現率・属性クロス集計・各コードの該当状況が正しく表示されること、コード共起ネットワークのiframe内DOMに`年配†`（ラベル付きコードノード）と`年配`（素の語ノード）が両方存在すること（`__data__`ではなく実際にレンダリングされた`.node-label`のテキストで確認）、凡例が「単語（コードへの接続少/多）」「コード」に正しく差し替わっていること、クラスター分析が「コード1件＋語8件」の混在ラベルで描画されること、「語も含める」をOFFにしてコード1件のみになった際に`len(labels) < 2`ガードが正しく発火し警告文が出てクラッシュしないこと、ページ全体に`StreamlitDuplicateElementId`等のエラー・トレースバックが一切表示されていないことを確認した。本体アプリ（`word-counter-demo`、ポート8502）は本セッション中いつの間にか停止していたため（他ポートでの検証作業に集中していた間）、ユーザーから「開かない」との報告を受けて再起動した。

- （続き）: ユーザーが実際に自身のブラウザで動作確認し「動作は確認しました」と一区切り。ただし設計上の懸念を提起——**「これまでは『単語』だけでできたデータ空間で作業してきました。しかし、単語とコードが混在では少し違和感が出てきます。」** 具体的な指摘内容はまだ詳しく聞けておらず、次回改めて設計を整理する予定（本セッションでは対応せず）。あわせて、次回は**LLMを使ったコード策定支援機能**（コードブックのトリガー語群をLLMに提案させる、`core/variant_grouping.py`の表記ゆれ提案と同様のパターンが応用できそうだが未検討）についても相談したいとの要望があった。

- （セッション再開）ユーザーから「昨日残した課題より先に、共起ネットワークの見直しをしたい。Rを扱えるか」と質問を受けた。KH Coderに同梱の古いR（`dep/R/bin/x64/Rscript.exe`、R 3.1.0、igraph導入済み）を発見・実行確認し、あわせてKH Coderの実際の描画バインディング`kh_lib/web_lib/forceNetwork-binding-0.4kh/forceNetwork.js`を読んだところ、標準的なD3 v4のforce simulationで、igraphによる座標事前計算は一切していないことが判明（`linkDistance=50`固定・`charge=-200`、既存3件のHTML実データの`nodes`にx/y座標が存在しないことも直接確認）。一方、ユーザーが見つけたブログ記事（`data-analyzer.net`の2記事、KH Coder公式ページも参照）から、`lay_f`という別系統の変数（igraphレイアウト座標）を使う**静止画出力**（Rの`plot.igraph`）の存在が判明。既に手元にあった`jaccard_3crass.png`等（同じ分析のKH Coder実機スクリーンショット）が実際にこの静止画出力だったため、Rを実行するまでもなく直接確認（`SendUserFile`でユーザーにも共有）——ハブが正三角形などに固定されず、次数の高い語が自然に中心付近へ集まる、有機的で不規則な配置だと判明した。ユーザーはこの見た目に寄せる方針を選択。同じ記事から、KH Coderが集計単位に「段落」（文書、現状のWord_Counterと同じ）と「文」（「。」区切り、より狭い共起判定——記事は「共起ネットワークだけは"文"の設定も必ず試すべき」と明言）を選べることも判明し、ユーザーがこの選択肢の追加も希望した。EnterPlanModeで2段階のExploreサブエージェント（既存コード調査）＋Planサブエージェント（設計レビュー）を経てから実装した。

**ハブ固定配置の撤廃（有機的なレイアウトへの刷新）**: [core/network_viz.py](core/network_viz.py)の`compute_layout`から、等角円配置（`_HUB_RADIUS_FACTOR`/`_HUB_CENTER_Y_SHIFT`）・`minx=maxx`によるハブ座標の厳密固定・ハブごとの小円クランプ（`_UNIT_HUB_LOCAL_RADIUS`）を全て削除。属性値ノードの有無に関わらず、グラフ全体（単語＋属性値）に対して一回だけ制約なしのFruchterman-Reingoldを実行し、キャンバスに正規化する方式に統一（旧`_layout_word_subgraph`を`_layout_via_fr`に改名・一般化してそのまま流用——二分岐だった構造が実質1本になりコード量も大幅減）。重み増幅（`_WEIGHT_SCALE=300`、Jaccard係数をFRの引力として十分な強さに引き上げる係数）はPlanエージェントのレビューで「ハブ固定を前提に導入された値であり、ハブ非固定の全ノード一括FRでは未検証」と指摘されたため、削除せず引き継いだ上で実データ相当の合成データ（1ハブに30語超が集中する偏りを含む）で再検証——ハブから語が無関係な位置へ飛ぶ症状（過去に発見・記録済みの不具合）は再発せず、そのまま採用した。
- **付随バグ修正（Planエージェントのレビューで発見）**: [ui/network_template.html](ui/network_template.html)のカスタム力`forceBoundary`が`node_type !== 'word'`で属性値ハブを除外していたため（ハブの安全な収まりは撤廃対象の`_HUB_RADIUS_FACTOR`頼みだった）、ハブ固定を撤廃すると単語ノードで過去に発生・修正済みの「キャンバス端はみ出しバグ」がハブノードで再発するリスクがあった。`forceBoundary`・`forceCollide`共通の`nodeHalfExtent(d)`ヘルパーを新設し（単語は円半径、属性値ハブは正方形の半辺`r×0.85`）、両方の力を属性値ハブにも適用するよう修正。D3側の`fx`/`fy`による属性値ハブの固定（Python側で固定しなくなったため不要になった）も削除。
- **配色をKH Coder実機の「Degree」凡例に合わせて刷新**: ノード色は、`attr_degree`（接続する属性値ハブの種類数）に基づく2点連続グラデーション（薄い青→濃い青）から、`d3.scaleSequential(d3.interpolateYlGn)`による次数ごとの離散段階（黄→緑）に変更。凡例も「少ない/多い」の2項目から、実際に出現する次数（1〜maxDegree）ごとに1行ずつ表示する方式に変更。
- **サイズ凡例の追加**: KH Coder実機同様、頻度→バブル直径の対応を具体的な数値目盛り（`maxFreq`の25/50/75/100%を丸めた値）付きの凡例で示す。凡例内では本編と別の縮小スケール（半径3〜13px）で円を描く（本編と同じ縮尺だと凡例内で巨大になるため）。凡例の行高さを項目ごとに動的計算するよう変更（従来は固定20px、サイズ凡例の円が大きい場合に収まらないため）。
- **不要になった表示文言引数の削除**: 上記の凡例刷新により、[ui/network_component.py](ui/network_component.py)の`build_network_html`の`attr_connection_low_label`/`attr_connection_high_label`引数（「単語（属性/コードへの接続少/多）」）が未使用になったため削除し、[ui/tab_crosstab.py](ui/tab_crosstab.py)の呼び出し側からも対応する2つのキーワード引数を削除した。

**集計単位（段落/文）の選択機能を追加**: [core/tokenizer.py](core/tokenizer.py)に`split_into_sentences(tokens)`を追加——トークン列を文末記号（`。！？…`、表層形での直接判定。POS側に句読点種別の判定ロジックが元々無いことを確認した上で、このプロジェクトの既存スタイル〈`_NEGATIVE_AUX_FORMS`等の小さなハードコード集合〉に合わせた）の直後で分割する。[core/network.py](core/network.py)に`expand_to_sentence_units(doc_tokens, doc_attrs)`を追加——文書ごとのトークン列を文単位に展開し、各文が元の文書の属性をそのまま引き継ぐ`(unit_tokens, unit_attrs)`を返す。`build_cooccurrence_edges`/`attr_value_doc_sets`自体は「文書」という概念に依存しない汎用設計のため無変更。[ui/tab_network.py](ui/tab_network.py)に「集計単位」セレクトボックス（`段落（文書）`/`文`、既定は段落＝現状通り）を追加し、「文」選択時は`unit_tokens`/`unit_attrs`を**両方セットで**`attr_value_doc_sets`/`build_cooccurrence_edges`に渡すよう変更（Planエージェントのレビューで、片方だけ差し替えるとインデックスがずれてJaccard交差が無意味な値になる落とし穴が指摘されたため、実装時に両者を同じ展開結果から取るよう徹底した）。ラベル文言（「最低出現文書数」等）・キャプション（「文書数: N」）も選択中の単位に応じて動的に切り替える。
- **検証**: `split_into_sentences`をスタンドアロンスクリプトで検証（複数文末記号混在、文末記号なし、連続する文末記号の縮退ケース、空リストの4パターン）。`expand_to_sentence_units`を2文書（属性値付き）の合成データで検証し、展開後のリスト長・属性の継承が正しいこと、実際に`build_cooccurrence_edges`/`attr_value_doc_sets`に両方揃えて渡して例外なく動作することを確認。`compute_layout`の刷新版を、1ハブに語が集中する偏りを含む合成データでスタンドアロンスクリプト検証し、例外なく完走・全ノードが点としてキャンバス内に収まる・ハブ間距離が不揃い（規則配置になっていない）・高次数ハブに繋がる語が無関係な位置へ飛んでいないことを確認。配色・サイズ凡例は一時的な検証用Streamlitハーネス（検証後削除）で`javascript_tool`を使い、凡例テキストが「接続属性数: 1/2/3」等の次数別表示になっていること、対応する次数のノードのstroke色が凡例の色と一致すること、サイズ凡例の目盛り（25/50/75/100等）と円半径が正しく描画されていることを確認。最後に、`app.py`と同じ`st.tabs`3タブ構成（共起ネットワークマップ・クラスター分析・コーディング集計）を再現したハーネスで全タブがエラー・トレースバック無く描画されることを確認した上で、tab_network.pyの内部ロジックを直接再現するハーネス（属性マッピング`職位`＋集計単位`文`＋バブルサイズ最大値3.0という最も厳しい組み合わせ）で、8文書→23文への展開、41ノード・44エッジのグラフ生成、そして**全41ノード（属性値ハブ3個の正方形を含む）の実際の見た目の外接矩形がキャンバス内に収まっている**ことを絶対座標変換で確認——`forceBoundary`拡張の効果を実データ相当の最悪ケースで検証できた。ハブ位置も不規則（等間隔でない）であることを実際の描画結果から確認した。

- （続き）: ユーザーが実データで「集計単位」「属性でマッピング」の組み合わせを一通り試したスクリーンショット6枚を共有。属性マッピングを使うケース（職位・所属学校、段落/文双方）は正しく動作していたが、**属性マッピング「なし」の2ケース（段落・文とも）で、凡例に本来出ないはずの「接続属性数: 1」という行が表示される不具合を発見**（スクリーンショットから気づいたユーザー指摘ではなく、こちらが精査して発見——ユーザーからの明示的な指摘ではないが実データで初めて可視化されたバグ）。

**次数凡例の誤表示バグを修正**: 原因は[core/network.py](core/network.py)の`build_graph`が、属性値ノードの有無に関わらず全ての単語ノードへ`attr_degree`を常に設定（属性値ハブが無ければ一律0）していたこと。[ui/network_template.html](ui/network_template.html)側の`degrees`算出が`d.attr_degree != null`だけで判定していたため、値が全て0であっても「次数データが存在する」と誤認識し（`0 != null`は真）、`hasAttrDegree`が常にtrueになって次数1件分の凡例行が出ていた（旧・2点連続グラデーション時代は「単語（属性への接続少/多）」という汎用文言だったため、この不具合が実質無害で見えていなかった——今回の次数別凡例〈具体的な数値を表示〉への刷新で初めて表面化した）。既に凡例セクションに定義されていた`hasAttrNodes`（属性値ノードが実在するかの判定）を先頭に引き上げて再利用し、`degrees`算出全体をこのガードで囲むよう修正——属性値ノードが1つも無ければ`degrees`は常に空配列になり、`hasAttrDegree`もfalseになる。
- **検証**: 一時的な検証用Streamlitハーネス（検証後削除）で、属性値ノード無し（単語×単語のみ、`attr_degree=0`を明示的に持つノードを含む——実際のバグ再現条件）と属性値ノード有りの2パターンを`javascript_tool`で確認——前者は凡例が「単語」のみ（「接続属性数」の行が消えたことを確認）、後者は従来通り「接続属性数: 1」「属性値」が正しく表示されることを確認。本体アプリ（`word-counter-demo`）を再起動し、`preview_logs`でエラー無いことを確認。

- （続き）: ユーザーがKH Coder実機のスクリーンショット（コンコーダンス画面から開いた共起ネットワーク、「カラー: サブグラフ検出（modularity）」のドロップダウンと、色ごとに視覚的にまとまったクラスターが並ぶ様子）を共有。あわせて3点の課題を指摘: ①属性なしの語×語ネットワークで「まとまり」が特に見えにくい（属性ありより問題が大きい）、②最大エッジ数を絞ると多少改善するが抜本的ではない、③バブル間の距離が短く密集感が強いことも視認性を下げている一因。加えて別メッセージで、KH Coder実機のバブル表現（外側=識別色の不透明な輪郭＋やや濃色の半透明塗り、内側=淡色の完全不透明な中心——エッジがラベル文字の下を通っても隠れる）を再現してほしいとの要望、および輪郭の太さの微調整（最初「補足」＝太くの指示、直後に実物を見て「細くしてください」と修正）があった。

**サブグラフ検出（Louvain法によるmodularityコミュニティ）カラーリングの追加**: [core/network_viz.py](core/network_viz.py)に`compute_communities(g)`を新規追加——igraphの`community_multilevel`（重み付きLouvain法）でノードをコミュニティに分割し、サイズの大きい順に0始まりで採番し直した`{ノードID: コミュニティ番号}`を返す（採番順を安定させ、実行のたびに同じ大きさのクラスタが同じ色にならない、という事態を避けるため）。ノードや辺が無い自明なグラフは全ノードを0にフォールバック。`graph_to_json_dict`に`communities`引数を追加し、各ノードJSONに`community`フィールドを埋め込む（未指定時は既定で全ノード0——[ui/tab_crosstab.py](ui/tab_crosstab.py)のコーディング集計タブは常に属性値ハブが存在し次数ベースの配色を使うため、communities計算自体を呼ばず既定値のまま無変更で動作する）。[ui/network_template.html](ui/network_template.html)側は、属性マッピングが無い（=次数ベースの配色が使えない）ケースで`d3.schemeTableau10`によるカテゴリカルなパレットを`community`値に割り当てる`useCommunityColor`分岐を`colorFor`に追加し、コミュニティが2件以上検出された場合のみ有効化（1件しか無ければ意味が無いため、従来通りの単色`WORD_HIGH`にフォールバック）。凡例にも「サブグラフ 1」「サブグラフ 2」…の行を追加。[ui/tab_network.py](ui/tab_network.py)の`_network_layout_cache`（グラフ構造・キャンバスサイズ単位でキャッシュする既存パターン）に`compute_communities`の結果もタプルで同居させ、`compute_layout`と同じタイミングで一度だけ計算・再利用する。

**バブルの二重構造化（KH Coder実機のスタイル再現）とエッジ被り対策**: [ui/network_template.html](ui/network_template.html)のノード描画を、単色30%不透明の円/正方形1枚だけだった従来版から、外側（識別色の半透明塗り`fill-opacity=0.35`＋識別色の不透明な輪郭`OUTER_STROKE_WIDTH`）と内側（識別色を`BG`方向へ72%寄せた淡色`paleColor()`の完全不透明な円/正方形、外側の60%＝`INNER_SCALE`のサイズ）の二重構造に変更——ラベル文字の下を通るエッジや半透明塗りが内側の不透明な淡色に隠れ、読みやすくなる。輪郭の太さはユーザーの実機確認を経て`1.2px`に調整（当初2pxを試したが「細くしてください」との指摘を受けて縮小）。凡例のcircle/square項目も同じ二重構造で描き直し、本編の見た目と一致させた。この変更に伴い、旧来ノード・凡例で共通して使っていた固定色`FILL_COLOR`（空色）は不要になり削除——塗り色は識別色（`colorFor(d)`／凡例の`item.color`）そのものに統一。

**密集感の緩和（キャンバス縦横独立フィット＋衝突解決の強化）**: ユーザー指摘の「バブル間の距離が短い」の原因を、①`compute_layout`がFRの自然な点群の縦横比を保ったまま`scale = min(avail_w/span_x, avail_h/span_y)`でキャンバスに収めていたため、点群とキャンバスの縦横比が合わないと片方の軸だけ余分に圧縮され実質的に詰まって見えていたこと（＝以前から指摩していた「無駄な余白」問題と表裏一体の症状だった）、②D3側の重なり回避`forceCollide`の余白が`+3px`とタイトで反復回数も既定の1回のみだったこと、の2点と特定。[core/network_viz.py](core/network_viz.py)の`_layout_via_fr`を、`scale_x = avail_w/span_x`・`scale_y = avail_h/span_y`を独立して適用する方式に変更（FRの力学的な距離に絶対的な縦横比の意味は無いため、縦横を別々にキャンバス一杯へ引き伸ばしても構造上の問題は無い——中心寄せの`offset_x`/`offset_y`計算も、独立スケールなら点群が正確に`[margin, width-margin]`を埋めるため不要になり削除）。[ui/network_template.html](ui/network_template.html)の`forceCollide`は余白を`+8px`に拡大し`.iterations(2)`を追加、`alphaDecay`も`0.05`→`0.03`に緩め、密集クラスタの重なりをより確実に解決するようにした。
- **検証**: `core/network_viz.py`の`compute_communities`/`compute_layout`/`graph_to_json_dict`をスタンドアロンスクリプトで検証——三角形2クラスタ＋弱い橋渡し辺1本の合成グラフで、期待通り2つのコミュニティ（0/1）に分かれること、`graph_to_json_dict`が各ノードJSONに正しい`community`整数値を埋め込むこと（`null`にならないこと）を確認。ブラウザでの実データ検証では、Streamlitのローカル開発サーバが`core/network_viz.py`の変更後もモジュールキャッシュを再読み込みしておらず（`ui/network_template.html`はファイルを都度`read_text()`するため反映されるが、Python側の`import`はプロセス起動時のまま）、ペイロードの`community`が終始`null`になる現象に遭遇——`preview_stop`/`preview_start`でサーバプロセスを再起動して解消した（Streamlitのローカルソースウォッチャーが常に確実とは限らない実例として記録）。再起動後、学校運営をテーマにした20文書の合成データ（属性マッピング無し）で、①27単語ノードが3つのサブグラフに分かれ`d3.schemeTableau10`の異なる色（`#4e79a7`/`#f28e2c`/`#e15759`）が凡例「サブグラフ 1/2/3」と対応して描画されること、②ノードの外側円（r=35.5, fill-opacity=0.35, stroke幅1.2）と内側円（r=21.3=外側の60%, 完全不透明の淡色）の二重構造が実際のDOM上に存在すること、を`javascript_tool`でのDOM属性検査と拡大スクリーンショットの両方で確認した。

- （続き）: ユーザーから3点の追加要望。①凡例を右上固定ではなく「タイトル下、左から凡例の幅分は描画しないように」——凡例の重なり問題（前回セッションから未対応のまま残っていた）に対する具体的な代替案として、位置を予測可能な左上に固定した上で、その矩形にノードを立ち入らせない除外ゾーン方式を提案された。②タイトルを英語「co-occurrence network map」＋プロジェクト名を横1行に統一（従来は太字タイトル・レギュラー太字のプロジェクト名の2段組）。③フォントサイズを現状より2pt程度縮小。

**凡例のタイトル下配置＋除外ゾーン方式による重なり回避**: [ui/network_template.html](ui/network_template.html)の凡例`translate(${width - legendWidth - 14}, 14)`（右上固定）を`translate(14, ${legendY})`（タイトル左端に揃え、タイトルのすぐ下）に変更。あわせて、`forceBoundary`（キャンバス端クランプ用に既存）に、タイトル＋凡例を合わせた矩形（`keepOutWidth`×`keepOutHeight`、モジュールスコープの`let`で宣言）にノードが入り込んだ場合、より近い辺（右または下）へ弾き出すロジックを追加——凡例の実際の位置・寸法が確定した時点（凡例構築コードの末尾）で`keepOutWidth`/`keepOutHeight`に実測値を代入するだけで済む。simulationの`tick`はスクリプト完了後に非同期（`requestAnimationFrame`駆動）で発火するため、開始時点で0でも実害はない。
- **副産物として発見した実データ上の重大なバグ（getBBox()のタイミング問題）**: 上記の除外ゾーンの寸法を`titleText.node().getBBox()`（タイトルの描画結果の実測）から算出する設計にしたところ、実際のStreamlitアプリで検証した際に凡例が`translate(14, 10)`という明らかに小さすぎる位置に描画される不具合が発覚。原因は、Streamlitの`st.tabs`が非アクティブなタブのiframeもDOMに存在させたまま裏で描画するため（`st.iframe`はタブが一度も選択されていなくても遅延生成される場合とそうでない場合があり、状況によって初回スクリプト実行時にタブがまだ非表示のことがある）、SVG要素が非表示の祖先を持つ状態で`getBBox()`を呼ぶと0が返ってしまうこと——タイトルの背景矩形がほぼ0サイズ（16×12px）で描画され、それを基準にした凡例位置・除外ゾームも巻き添えで壊れていた。この問題は実は今回新規に持ち込んだものではなく、旧・2段組タイトルの背景矩形サイジングでも同じ`getBBox()`依存があったため元々存在していたが、無害なズレ（背景矩形が少し小さいだけ）で済んでいたのに対し、今回は凡例位置・除外ゾーンという構造的に重要な値の計算に使ってしまったため症状が顕在化した。
- **対策**: `getBBox()`への依存を完全に排し、canvas 2Dコンテキストの`measureText()`でタイトル文字幅を測定する`measureTextWidth(text, fontSize, fontWeight)`ヘルパーを新設——canvasでの文字幅測定は要素の表示状態（`display:none`祖先の有無）に左右されないため、タブが非表示の状態でスクリプトが実行されても正しい値を返す。タイトルの背景矩形の高さは、フォントサイズが固定（14px）であることを踏まえた固定オフセット定数（`TITLE_TOP=5`, `TITLE_HEIGHT=20`——可視状態での実測値を基準にした値）で代替した。

**タイトルの1行化・英語化・縮小**: [ui/network_template.html](ui/network_template.html)のタイトル描画を、`titleText`（太字16px）＋別要素`subtitleText`（レギュラー12px、2段目）の2要素構成から、`titleText`1要素＋`tspan`（レギュラー10px、` / プロジェクト名`）の1行構成に変更。あわせてタイトル本体のフォントサイズを16px→14pxに縮小（ユーザー指定の「2ポイント程度」に対応）。[ui/tab_network.py](ui/tab_network.py)の`build_network_html`呼び出しで`title='共起ネットワーク'`から`title='co-occurrence network map'`に変更（`ui/tab_crosstab.py`のコーディング集計タブ側は対象外・無変更）。
- **検証**: Streamlitの`st.text_input`が本セッション中に断続的にコミットされない現象（JSキーイベント合成・`form_input`経由の値設定いずれでも、ブラウザ側DOM値は更新されるがReact側の内部状態に反映されずサーバへ送信されないケースがあった）に阻まれ、実アプリでの対話的な再現がしづらかったため、`core/network_viz.py`の関数を直接呼び出して実際の`build_network_html`出力を生成するスタンドアロンスクリプトで検証用HTML（3クラスタ・10ノードの合成データ、タイトル`'co-occurrence network map'`・プロジェクト名`'対話がない理由'`）を生成し、プロジェクトディレクトリ内に一時配置した上で`python -m http.server`経由で提供して`javascript_tool`・スクリーンショットで検証（`file://`直開きはBrowserペインの静的スナップショット制限でスクリプトが実行されないため不可と判明）。結果、①タイトルが`"co-occurrence network map / 対話がない理由"`という1行のテキストノード（`tspan`込み）としてレンダリングされること、②凡例の`transform`が`translate(14, 35)`（タイトル直下・左揃え）になっていること（修正前は`translate(14, 10)`という壊れた値だった）、③タイトル背景矩形の幅が`measureTextWidth`ベースで実測幅とほぼ一致する286px（従来の`getBBox()`ベースでは16pxまで縮退していた）になっていること、④実際に描画された10ノードが全てタイトル・凡例の矩形範囲外に収まっていること（スクリーンショットで目視確認）、を確認した。検証用HTMLファイル・一時HTTPサーバは検証後に削除・停止した。

- （続き）: ユーザーからテスト結果報告前に「凡例の文言を短くして幅を狭めたい、サブグラフの名前を『class1』などにしたいが不適切か」との質問。回答として、「class」は教師あり学習の既知カテゴリを連想させ、教師なしのコミュニティ検出結果（データから偶然見つかった、名前のない集団）を指すには紛らわしい旨を説明し、代替案として「群1」「C1/G1」を提示、あわせて`compute_communities`のロジック（Louvain法→サイズ順の再採番）を説明した。ユーザーが「group1」を選択、続けて「共起が弱い/共起が強い」も「共起弱/共起強」への短縮を提案。

**凡例ラベルの短縮＋凡例幅の動的化**: [ui/network_template.html](ui/network_template.html)の凡例ラベルを`サブグラフ ${c+1}`→`group${c+1}`、`共起が弱い`/`共起が強い`→`共起弱`/`共起強`に変更。ラベルを短縮しても`legendWidth`が`190`の固定値のままでは箱の幅自体は狭まらない（右側の余白が増えるだけ）ことに気づき、タイトル幅算出に使っていた`measureTextWidth`ヘルパーを流用して、全凡例アイテムのうち最長のラベル幅から`legendWidth`を動的に算出するよう変更（アイテム種別を問わずラベルは常に`legendPadding+22`の位置から始まるため、`legendTextX + maxLabelWidth + legendPadding`で幅を決定、最小値70pxを保証）。
- **検証**: 前回と同じスタンドアロンHTML生成＋`python -m http.server`方式で、①凡例の全ラベルが`group1`/`group2`/`group3`/`共起弱`/`共起強`になっていること、②凡例の幅（`stroke="#222222"`のrect要素の`width`）が旧来の190pxから約79pxまで縮小していること、をDOM属性検査とスクリーンショットで確認した。検証用ファイル・一時サーバは検証後に削除・停止した。

- （続き）: ユーザーから最後にサイズ凡例について2点の指摘。①「実際の円に付ける数値が個々に違う（半端な値になる）のではなく、10/20/50/100等のきりの良い数値にできないか」、②「プロットはズーム・パンで拡大縮小できるが、この凡例の表記方法（固定ピクセルサイズ↔具体的な数値の対応）は有効なのか」。後者について、①凡例の参照円は元々ズーム非対応の固定オーバーレイであり本編のバブルとは別の圧縮スケールを使っていること、②ズームは全ノードを一様に拡大縮小するため大小関係の比較自体はズーム後も有効なままだが、ピクセルサイズと具体的な数値の1:1対応は初期表示（ズーム倍率1.0）でのみ厳密に成立すること、を説明。ズームに追従して凡例側も動的リサイズする案（正確だがレイアウトが複雑化）と、現状維持のまま注記を足す案を提示したところ、ユーザーから「実際の円は段階的（5段階等）に作成されているか」と質問があり、現状は連続スケール（`d3.scaleLinear`、離散的な区分けなし）である旨を回答。続けてユーザーから「表現だけで解決したい」という方針のもと、**最小・中間・最大を表す同心円（三重丸）を描画し、きりの良い数値を添えるだけで良いのでは**との提案があり、採用した。

**サイズ凡例を同心円（nested circle legend）＋きりの良い数値に刷新**: [ui/network_template.html](ui/network_template.html)の`niceSizeTicks`（`maxFreq`の25/50/75/100%を単純に丸めるだけで半端な数値になっていた）を`niceMinMidMax`に置き換え——d3標準の`scaleLinear().nice().ticks()`（1/2/5×10^nパターンでの「きりの良い」目盛り生成）を使い、0より大きい目盛りから最小・中間（配列長の中央インデックス）・最大の3点を選ぶ。この3点を、従来のように個別の行に並べるのではなく、**共通のcx・共通の底辺(baseline)を基準に配置した同心円1つのグラフィック**（最小の円が一番下・最大の円が一番上に来る「入れ子」構造）としてまとめ、各円の頂点（`baseline - 2r`）から水平の引き出し線を伸ばした先に数値ラベルを添える——KH Coder含む統計図表で一般的な「nested circle legend」の描き方（従来の3〜4行の個別サイズ行より大幅に省スペース）。凡例の幅・高さの計算（`itemRequiredWidth`/`rowHeights`）も、この同心円グラフィックが必要とする実際の幅・高さ（円のブロック＋引き出し線＋最長の数値ラベル幅）を考慮するよう対応する分岐を追加した。ズームとの整合性の限界（初期表示スケールでのみ厳密に一致）はコード側のコメントとして明記し、実装自体は変更しない（ユーザーの「表現だけで解決したい」という方針通り）。
- **検証**: 頻度にばらつきを持たせた合成データ（`max_freq=47`）で前回と同じスタンドアロンHTML生成方式により検証——d3の`nice().ticks()`が`maxFreq=47`から`[10, 30, 50]`という きりの良い3点を選んでいること、3つの参照円が全て同じ`cx`（共通中心）を持ち、`cy+r`（円の下端）が3つとも同一値（132.0）で揃っている——同心円として正しく底辺が揃っていること、をDOM属性検査で確認。あわせてSVG全体を3倍拡大したスクリーンショットで、引き出し線・数値ラベル（50/30/10）が重ならず正しく配置されていることを目視確認した。検証用ファイル・一時サーバは検証後に削除・停止した。

- （続き）: ユーザーが実データのスクリーンショットを共有——属性なしの共起ネットワークマップは「大変良好」と確認が得られた（52単語ノード・80エッジ・5サブグラフ、タイトル1行化・凡例位置・二重円バブルとも意図通り動作）。ダウンロード・原文クリック表示は別課題として残し、属性ありのケースを次に見る予定とのこと。直後、「凡例内の三重丸表現で、数値が若干重なっている」との指摘があった（実データでは`niceMinMidMax`が選んだ値同士の`legendSizeScale`上の半径差が小さく、円の頂点＝引き出し線の起点同士が近接し、10px文字のラベルが重なっていた）。

**同心円サイズ凡例のラベル重なりを修正**: [ui/network_template.html](ui/network_template.html)に`nestedSizeLayout(vals)`ヘルパーを新設し、円の半径・実際の頂点位置（`naturalTopY`、baseline=0基準の相対値）はそのまま維持しつつ、ラベルの縦位置（`labelY`）だけ底辺側から順に最小間隔`NESTED_MIN_LABEL_GAP=13px`（10px文字が重ならない間隔）を確保するよう押し上げるロジックを追加。引き出し線は「本来の頂点（naturalTopY）」から「調整後のラベル位置（labelY）」へ引くため、間隔調整が入った場合はわずかに斜めの線になる（大きな調整が入るケースは稀で、見た目に不自然にはならない）。この関数は凡例の行の高さ算出（`rowHeights`、調整後の最上部ラベルまでの高さ`topExtent`を反映——旧実装は円の自然な高さしか見ておらず、ラベル調整で必要になる追加の高さを考慮していなかった）と、実際の描画の両方で共通利用し、二重計算・結果のズレを防いだ。
- **検証**: 半径差が小さくなるよう意図的に偏らせた合成データ（頻度58/60/30/15/5、`max_freq=60`）で前回と同じスタンドアロンHTML生成方式により検証——修正前なら重なっていたはずの3つのラベル（60/30/10）のy座標が、実際に描画された`<text>`要素から`89, 102, 115`（隣接差はいずれもちょうど`13px`＝`NESTED_MIN_LABEL_GAP`）になっていることをDOM属性検査で確認し、3倍拡大スクリーンショットでも重なりなく配置されていることを目視確認した。検証用ファイル・一時サーバは検証後に削除・停止した。

- （続き）: ユーザーが職位/所属学校/年代の3種類の属性マッピング結果をスクリーンショットで共有——「間違いではないが、全体的に空白が多く距離感や密度がバラバラで美しくない」との指摘。3枚とも共通して、属性値ハブ（数個）とその語群が画面の一部（主に左寄り）に偏り、キャンバスの残り半分近くが空白になっていた。

**属性マッピング時のレイアウト不均一を`niter`引き上げで改善**: 原因調査のため、実データに近い合成データ（属性値ハブ3個・不均等な語数配分〈65%が中心ハブに接続〉、`core/network.py`の実関数〈`build_cooccurrence_edges`/`build_graph`〉を通した35ノード・56エッジのグラフ）を作り、スタンドアロンHTML生成で再現に成功——1つのハブが少数の弱い橋渡し辺のみで他から離れ、残りが1箇所に密集する「偏った不均一な配置」が実際に再現された。対策候補としてigraphの`layout_kamada_kawai`への切り替えを試したが、この構造（属性値ハブ数個への二部グラフ、word-word辺なし）ではハブ同士が重なるなど改善が不十分だった。次に`layout_fruchterman_reingold`の`niter`（既定500）を引き上げたところ、`niter=3000`で3つのハブが三角形状に均等に分散し、空白が大きく減少することを確認——`niter=5000`との比較でも既に収束しており、これ以上の追加効果はほぼ無かった。計算コストも実データ最大規模相当（170ノード・300エッジ）の合成データで約0.24秒と軽微。[core/network_viz.py](core/network_viz.py)の`_layout_via_fr`の`layout_fruchterman_reingold`呼び出しに`niter=3000`を明示指定した。
- **検証**: 上記の同じ合成データ（属性マッピング相当）で`niter=3000`により改善を確認（3ハブの三角形状の分散、空白の大幅減少）。既に「大変良好」と確認済みだった属性なし（単語のみ）の合成データでも同じ`niter=3000`で再検証し、レイアウト・コミュニティ色分けとも変化なし（回帰なし）であることを確認した。実データでの最終確認はユーザーに委ねる——Streamlitのローカル開発サーバは`core/network_viz.py`のようなPythonモジュール変更をプロセス再起動なしには確実に反映しないことが既に判明していたため（本セッション既出の既知の落とし穴）、`preview_stop`/`preview_start`でサーバプロセスを再起動した。検証用ファイル・一時サーバは検証後に削除・停止した。

- （続き）: ユーザーが`niter=3000`適用後の実データスクリーンショットを共有——「円形に配置してほしいのに縦方向や横方向に直線的にバブルが配置されるケースが多くまとまりがない」との指摘。ハブ間の空白は改善したが、今度は個々のハブ周りの語群が円状ではなく細長い扇状・線状に伸びる問題が残っていた。

**属性マッピング時の語群の直線化を`_WEIGHT_SCALE`引き下げで改善**: 同じ実データ相当の合成グラフ（3ハブ・35ノード）で、Kamada-Kawai法への切り替えを再度検討したが、KKは今回もハブ同士がほぼ重なる副作用が出たため不採用。代わりに引力の強さ（`_WEIGHT_SCALE`、元は300）を疑い、150・100の2案をテスト——引力が強すぎると、ハブ1つに繋がる語同士の相対的な反発力が弱まり、円状ではなく狭い扇状・線状に押し込まれることが判明した。150に下げたところ、3ハブそれぞれの語群が丸く展開し、ハブ全体も三角形状に配置される、明確に改善した結果が得られた（100でも同様に良好、150の方がやや保守的な下げ幅として採用）。[core/network_viz.py](core/network_viz.py)の`_WEIGHT_SCALE`を`300.0`→`150.0`に変更。
- **既存の不具合再発防止の再検証**: `_WEIGHT_SCALE`のコメントには「値が小さすぎるとハブに語が集中する場合に反発力に負けて無関係な位置まで散らばる」という過去に発見済みの不具合が明記されていたため、この引き下げでその症状が再発しないか、より極端な合成データ（1ハブに45語が集中、別の1ハブに3語という不均衡な配置）で別途ストレステストを実施——語は無関係な位置に飛ばず、集中したハブの周りに丸くまとまることを確認し、再発しないことを確認した。既に確認済みだった属性なし（単語のみ）の12ノード合成データでも同じ`_WEIGHT_SCALE=150`で再検証し、レイアウトに変化がない（回帰なし）ことを確認。実データでの最終確認はユーザーに委ねる——サーバプロセスを再起動済み。検証用ファイル・一時サーバは検証後に削除・停止した。

- （続き）: ユーザーが`niter=3000`＋`_WEIGHT_SCALE=150`適用後、属性なし×集計単位2種・属性=職位×集計単位2種の計4パターンのスクリーンショットを共有——「全て表示されてすぐの画像」（≒初回描画時点、キャッシュなし）という条件で、4枚中2枚（属性なし×段落、属性=職位×文）は「もう少しまとまりが欲しい」、残り2枚（属性なし×文、属性=職位×段落）は良好、との評価だった。

**igraphのFR非決定性への対策としてbest-of-N選択方式を導入**: 4パターン中2パターンだけ悪い結果になったことから、`igraph.layout_fruchterman_reingold`が同一seedでも呼び出すたびに異なる結果を返す非決定性（`_layout_via_fr`の既存コメントに明記済みの既知の特性）が原因と推定——同じグラフ構造・パラメータでも「良い配置」と「線状・扇状に偏った配置」が運次第で出てしまっていた。対策として、[core/network_viz.py](core/network_viz.py)の`_layout_via_fr`を、FRレイアウトを1回だけ計算する方式から、**`_LAYOUT_TRIALS=5`回計算し、最も見た目の良い結果を選ぶ**方式に変更した。「見た目の良さ」は2つの指標の順位の合計（小さいほど良い）で判定する: ①`_edge_crossing_count`（線分交差数、グラフ描画で定番の「わかりやすさ」指標——端点を共有する辺同士は交差とみなさない）、②`_nearest_neighbor_cv`（各ノードの最近傍距離の変動係数——値が小さいほどノードが均等に広がっており、線状・扇状の偏りが起きていないことを示す）。両指標はキャンバスへの正規化（縦横独立スケール）の影響を受けない生の計算結果（`layout.coords`）に対して計算する——アフィン変換（縦横独立スケールを含む）は線分の交差関係を変えないため、正規化前後で同じ結果になる。
- **検証**: 実データに近い合成グラフ（属性値ハブ3個・35ノード・56エッジ）で、①`compute_layout`を独立に4回呼び出し、それぞれ選ばれた候補の交差数・最近傍距離変動係数を計測——毎回ほぼ同じ値（交差数68〜70、変動係数0.238〜0.257）に収束し、単発計算だけを行っていた場合よりも明らかにばらつきが小さくなっていることを確認した（単発計算8回の交差数のばらつきは66〜70——似た範囲に見えるが、こちらは「たまたま」であり、選択方式は常に妥当な候補を選べていることが変動係数の安定性からより明確に確認できた）。②実際に生成したHTMLをスタンドアロンHTTPサーバ経由でスクリーンショット確認し、3ハブが三角形状に配置され、語群も丸く展開していることを目視確認した。③計算コストは実データ規模（35ノード）で0.07秒、実データ最大規模相当（170ノード・392エッジ）でも5試行で約2秒未満（Streamlit側は`_network_layout_cache`によりグラフ構造が変わらない限り再計算しないため、通常の対話操作では体感コストにならない）。検証用ファイル・一時サーバは検証後に削除・停止し、`preview_stop`/`preview_start`でサーバプロセスを再起動した。実データでの最終確認はユーザーに委ねる。

- （続き）: ユーザーが属性なし・属性=職位・属性=所属学校（各×集計単位2種、計6パターン）のスクリーンショットを共有——「4属性（所属学校）も含めた。最初の属性なし（段落）は改善が必要、その他は全体的にほんの少し窮屈だが概ね良好」との評価。「属性なし・段落」のスクリーンショットには、弱い橋渡し辺1本（係数0.05）だけで全体と繋がる3語の小クラスタ（「話す/気/対する」）が、キャンバスの反対側まで孤立して大きな空白ができている問題が写っていた。

**孤立した弱連結クラスタの空白問題は`_LAYOUT_TRIALS`の試行選択では解決できないことが判明**: 同じ「弱い橋渡し辺1本で繋がる3語クラスタ」を模した合成グラフで、同一構造に対し独立呼び出しを6回試したところ、孤立距離（クラスタの重心と本体の重心の距離）は毎回ほぼ同一値（4.18前後）だった——igraphのFR非決定性はこの孤立現象そのものには実質的に効かず、弱い橋渡し辺1本という構造そのものに起因するほぼ決定的な結果であることが分かった（`_LAYOUT_TRIALS`回の試行選択だけでは解決できない）。一方、引力係数（`_WEIGHT_SCALE`）を上げるとこの孤立距離は縮む（同じ合成データで150→4.18、300→3.32、という具合に約2割減）ことを確認——ただし引力係数を上げすぎると、前回対策した属性マッピング時の「ハブ周りの語が線状に並ぶ」不具合が再発するリスクがある（単一の孤立スター構造での検証では明確な悪化は見えなかったが、実際に問題が起きた「複数ハブが競合する」構造でのみ顕著だった経緯を踏まえ、安全側に倒した）。

**引力係数をグラフの種類（属性値ハブの有無）で使い分けるよう変更**: [core/network_viz.py](core/network_viz.py)の`_WEIGHT_SCALE`（単一定数）を`_WEIGHT_SCALE_ATTR=150.0`（属性値ハブがあるグラフ用、既存の値を維持）と`_WEIGHT_SCALE_WORD=300.0`（属性値ハブが無い語×語ネットワーク用、元の値に戻す）の2つに分割した。`_layout_via_fr`で`g.nodes[node].get('node_type') == 'attr'`のノードが1つでもあるかどうかを見て、どちらの係数を使うか自動判定する。「ハブに語が線状に並ぶ」問題（属性マッピング時特有）と「弱連結クラスタが孤立する」問題（語×語ネットワークで発生、ハブという概念自体が無い）は別々のグラフ構造・別々の症状であり、片方の対策がもう片方に悪影響を与えない前提に基づく設計判断。
- **検証**: 実際の`core/network_viz.py`の`compute_layout`関数（スタブではなく実装そのもの）に、上記の孤立クラスタ構造を持つ`nx.Graph`（属性値ハブ無し）を通し、`_WEIGHT_SCALE_WORD=300`が実際に適用されていること・実際のキャンバス座標での孤立距離（575px、900pxキャンバス幅に対して依然として大きいが、この孤立は係数0.05という実際に弱い関係性を正直に反映した結果であり、無理に近づけるとデータを誤って表現することになる点はユーザーに説明する）を確認した。属性マッピング時（`_WEIGHT_SCALE_ATTR=150`、3ハブの合成データ）は変更の影響を受けず、既存の丸い扇状配置が維持されていることも再確認した。検証用ファイル・一時サーバは検証後に削除・停止し、サーバプロセスを再起動した。実データでの最終確認はユーザーに委ねる。

- （続き）: ユーザーから「なんとか解決できないか？一定以下の数値は切り上げるだとか」との提案。

**Jaccard係数の下限floorを試したが、キャンバス正規化の仕組み上ほぼ効果が無いことが判明**: 提案通り、レイアウト計算専用に「一定以下の係数は切り上げる」`_MIN_EDGE_WEIGHT`（下限floor、表示上のエッジ太さ・ラベル数値には影響しない）を実装し、`max(w, _MIN_EDGE_WEIGHT) * weight_scale`という形で適用。生のFR座標上ではfloorを上げるほど孤立距離が縮む（floor=0→3.32、0.25→2.56、0.4→さらに減）ことを確認したが、**実際にキャンバスへ正規化した後の最終的な孤立距離ではほぼ効果が無かった**（floor=0でも0.4でも556〜575px程度でほぼ変わらず）。原因を調査したところ、[core/network_viz.py](core/network_viz.py)の縦横独立スケール（点群の外接矩形をキャンバスいっぱいに引き伸ばす方式）が、孤立ノードを含む外接矩形を基準にスケール自体を計算しているため、floorで孤立ノードの生座標上の距離を縮めても、外接矩形全体も連動して縮小し、結果的にスケールが大きくなって「見た目の孤立度合い」を相殺してしまうことが分かった——正規化「前」の座標に対する補正は、この仕組みのせいでほぼ無意味になる。

**キャンバス正規化「後」の座標に対するoutlier引き寄せ処理を追加**: 上記の知見を踏まえ、`_MIN_EDGE_WEIGHT`は控えめな値（0.15、実データの係数分布0.045〜0.73のうち最も弱い部分だけに影響）として残しつつ、[core/network_viz.py](core/network_viz.py)に`_pull_in_outliers(positions)`関数を新設し、`_layout_via_fr`の最後（キャンバス正規化が完了した「後」）に適用する方式に変更した。全ノードの重心距離の中央値を基準に「外れ値」の閾値（中央値の1.3倍、`_OUTLIER_THRESHOLD_MULT`）を決め、それを超えるノードだけを超過分の60%（`_OUTLIER_PULL_FACTOR`）だけ重心方向へ引き寄せる——正規化後の座標を直接補正するため、スケール自体には影響を与えず、狙った通りの視覚的な孤立緩和になる。中央値ベースの閾値なので、属性値ハブのように重心から自然にある程度離れているノードは影響を受けない。
- **検証**: 同じ孤立クラスタ構造の合成データで、正規化後の座標に対する引き寄せ処理により、最終的なキャンバス上の孤立距離が575px→466px（約19%減）に改善することを実際の`compute_layout`関数で確認。属性マッピング時の3ハブ合成データでは、各ハブの重心距離（134.7〜197.0px）が外れ値閾値（中央値×1.3＝約330px）を大きく下回ることを確認済みで、実際にスタンドアロンHTMLをレンダリングして三角形状の配置・丸い扇状展開が変更前と変わらず維持されていることを目視確認した。孤立クラスタ側も、レンダリングして「対する/気/話す」が主要クラスタに以前よりも近づいていることを目視確認——ただし完全な統合ではなく、あくまで緩和に留まる（弱い関係性を誤って強く見せないため、意図的に抑えた設計）。検証用ファイル・一時サーバは検証後に削除・停止し、サーバプロセスを再起動した。実データでの最終確認はユーザーに委ねる。

- （続き）: ユーザーが実データで4パターンを確認——「ゴール達成は近い」との前向きな評価を得つつ、2点の微調整依頼があった。①属性なし・段落のケースで「離れ小島（孤立クラスタ）が近づきすぎた」——`_OUTLIER_PULL_FACTOR=0.6`が実データでは強すぎ、弱い関係性が実際より強く見えてしまっていた。②属性=職位・段落のケースで「全体をほんの少し右に、左端がラベルで見切れている」——属性値ハブの長い文言（例:「学校事務職員・その他」）のラベルが、ハブがキャンバス左端近くに来た際にキャンバス外へはみ出して見切れていた。

**孤立引き寄せの緩和とマージン拡大**: [core/network_viz.py](core/network_viz.py)の`_layout_via_fr`の`margin`を60→90に拡大——`forceBoundary`（D3側）はノードの円・正方形の半径分しか余白を見ておらず、その外側にはみ出すラベル文字幅を考慮していないため、属性値ハブの長い文言がキャンバス端近くで見切れる問題への対策。あわせて`_OUTLIER_PULL_FACTOR`を0.6→0.15に緩和——ただし検証の過程で、marginの拡大自体もキャンバスの使用可能領域を狭めるため、全ての距離（孤立ギャップを含む）を比例して縮める副作用があり、0.35程度への緩和ではmarginの影響と相殺してほぼ変化が無いことが判明。0.15まで踏み込んで緩和し、正味で「以前より離れて見える」効果を確保した。
- **検証**: 同じ孤立クラスタ構造の合成データで、`margin`・`_OUTLIER_PULL_FACTOR`それぞれを個別に変化させた組み合わせを比較し（margin=60/factor=0.6→466px、margin=90/factor=0.6→424px、margin=90/factor=0.35→462px、margin=90/factor=0.15→492px、margin=90/factor=0→514px〈引き寄せ完全無効〉）、実際に採用したmargin=90・factor=0.15の組み合わせで、実際の`compute_layout`関数から最終孤立距離が492px（以前の「近づきすぎ」状態466pxより明確に離れ、無対策の575pxよりは改善、という中間的な結果）になることを確認。属性マッピング時の3ハブ合成データもmargin=90で再レンダリングし、最も左のノードがx=94px（キャンバス左端から十分な余白）に収まり、ラベルの見切れが解消されていることをスクリーンショットで確認した。検証用ファイル・一時サーバは検証後に削除・停止し、サーバプロセスを再起動した。実データでの最終確認はユーザーに委ねる。

- （続き）: ユーザーが属性なし・段落のケースを再確認——「惜しいです。ほんの少し下に」との指摘。実際の画面では、タイトル・凡例（左上固定オーバーレイ）の分だけ上側に余白が必要な一方、下側は同じ余白を残すと全体が上に偏って見えていた。

**上下マージンを非対称化して全体を少し下に**: [core/network_viz.py](core/network_viz.py)の`_layout_via_fr`で、これまで上下左右均等だった`margin`（90）を、上下だけ`margin_top=115`・`margin_bottom=65`という非対称な配分に変更（合計は左右と同じ180のまま、配分だけ上に多く・下に少なく）。左右のマージンは変更なし（90のまま）。
- **検証**: 実際の`compute_layout`関数に多少複雑な合成グラフ（15ノード）を通し、y座標の範囲が期待通り`[margin_top=115, height-margin_bottom=635]`の範囲に収まっていることを確認した（3ノードのみの極小グラフでは点群がほぼ1点に潰れる退化ケースが起きたが、これはこの変更固有の問題ではなく、そもそも自明に小さいグラフでのFR収束の一般的な性質）。サーバプロセスを再起動し、実データでの最終確認はユーザーに委ねる。

- （中略・以後複数ラウンド）: 上記の後もキャンバス余白・マージン（左右非対称化を含む）・孤立クラスタ緩和は実データのスクリーンショットを見ながら細かく調整を重ねた。並行して、KH Coder実機の静止画出力（`plot.igraph`）を調査した結果、属性値ハブを正三角形等に固定配置する従来方式ではなく、ハブも含めた全ノードを1回のFruchterman-Reingoldで一括レイアウトする有機的な配置の方がKH Coder実機に近いと判断し、`compute_layout`のハブ固定ロジックを撤廃した。色分けも次数（接続する属性値ハブの種類数）ごとの離散パレットに刷新し、共起の集計単位を「段落（文書）」「文」から選べるセレクタを追加した。ユーザーは最終的に「どうしても、どこかの端に寄ってしまう」ことを許容し、完全自動での偏りゼロ配置は「抜本的な対策（手動ドラッグ配置等）が必要になりそうだが、有料化する際まで保留、当面は手動移動で運用する」と結論。

### ダウンロード機能（PNG等倍・PNG2倍・SVG）

ユーザーから「見た目と同じ解像度と倍の解像度のPNG、ベクター系のデータが必要」との要望。サーバサイドで画像を再生成するのではなく、**画面に描画済みのD3のSVG DOM自体をクライアント側でシリアライズしてそのまま書き出す**方式を採用した（見た目との完全な一致を保証しやすく、Python側の実装追加も不要なため）。[ui/network_template.html](ui/network_template.html)に右下固定の`#downloadToolbar`（PNG／PNG(2x)／SVGの3ボタン）を追加し、以下のJS関数群を実装した。

- `buildExportSvgString()`: 表示中の`<svg>`をクローンし、ページ本体の`<style>`（CSSクラス）は書き出し単体のSVGファイルには適用されないため、ラベル配置・エッジ係数ラベルの見た目を保つ最小限のCSSを`<style>`としてSVG自身に埋め込んでから`XMLSerializer`で文字列化する。
- `downloadSvg()`: 上記の文字列をそのまま`Blob`化してダウンロード。
- `downloadPng(scale)`: SVG文字列をBlob URL化→`Image`に読み込み→`scale`倍したサイズの`<canvas>`に背景色を塗った上で描画→`canvas.toBlob('image/png')`でダウンロード（`scale=1`で等倍、`scale=2`で倍解像度）。
- ファイル名は`タイトル_プロジェクト名`（不正文字はサニタイズ）。

`st.iframe`のsandbox設定がクライアント側ダウンロード（Blob URL＋`<a download>`のクリック）を許可するかが懸念点だったが、実際にStreamlit上で動作確認したところダウンロードは問題なく機能した。

**ダウンロード後の実データフィードバックで発覚した2件の不具合を修正**:
1. **Illustratorでのフォント警告**: 書き出したSVGをAdobe Illustratorで開くと、`Noto Sans JP`・`Hiragino Kaku Gothic ProN`について「原因不明の問題が発生しました」という警告が毎回出た。両フォントとも元々Windows上には存在せず、画面表示でも常にMeiryoへフォールバックしていた（webフォント埋め込みなし）ため実質的に無意味な指定だったと判明——`buildExportSvgString`内の書き出し専用スタイルからのみこの2つを削除し、`Meiryo, "MS Gothic", sans-serif`に絞った（画面表示側の`<style>`は変更なし、見た目に影響なし）。
2. **係数ラベルの縁取りによるエッジの見かけ上の分断**: 同じくIllustratorで確認したところ、エッジ（`<line>`）の途中が切れているように見えた。原因は係数ラベル（`.edge-label`）の`paint-order: stroke; stroke: 背景色; stroke-width: 3px`という、画面上でエッジと重なった際の可読性のための背景色縁取り——ベクター編集アプリではこの縁取りが背景色の別要素として認識され、エッジがラベルの真下で途切れて見えてしまう。書き出し専用スタイルの`.edge-label`からは縁取り（`paint-order`/`stroke`/`stroke-width`）を外し、塗り色のみのシンプルなテキストにした（画面表示側は可読性のため縁取りを維持、変更なし）。エッジ自体は元々単一の`<line>`要素であり、データ構造上は分断されていなかったことも確認済み。
- **検証**: 属性値ハブ3個・語10個の合成データで、スタンドアロンHTML生成＋`python -m http.server`方式によりブラウザで直接確認——SVGダウンロードボタンのクリックが例外なく完了すること、書き出し用`.edge-label`スタイル文字列に`stroke`関連プロパティが含まれないこと（ソースの該当行を直接確認）、画面表示側の`.edge-label`（縁取りあり）には変更が及んでいないことを確認した。検証用ファイル・一時サーバは検証後に削除・停止した。

**次数カラーパレットを黄緑系から青系に変更**: ユーザーから「属性付き出力の際、グリーン基調になっているが、ブルー基調に変更してほしい」との依頼。[ui/network_template.html](ui/network_template.html)の`wordColorScale`が使う補間関数を`d3.interpolateYlGn`（黄→緑）から`d3.interpolateBlues`（淡→濃の青）に変更（次数ごとの離散段階という設計自体は変更なし）。
- **検証**: 属性値ハブ3個・語10個の合成データでスタンドアロンHTML生成方式により再検証——次数1/2/3の単語ノードが淡青・中間青・濃青の3段階になっていることをスクリーンショットで確認し、係数ラベルに縁取りが無くなっていることもあわせて目視確認した。検証用ファイル・一時サーバは検証後に削除・停止した。
- **補足**: 色を決める`wordColorScale`は画面表示用SVGと書き出し用SVGの両方で完全に同一のコード（書き出しは画面上のライブSVGをそのままクローンする方式のため）。ただし`st.iframe`はテンプレートファイルの変更だけでは自動再描画されず、Streamlitが次に再実行（rerun）されるまで古い描画が画面に残り続ける点に注意——ユーザーが「画面はまだ緑」と感じた場合も、ページ再読み込みや設定変更によるrerunで解消する。

以上により、共起ネットワークマップ機能（レイアウト・配色・凡例・ダウンロード）は一区切りとする。残課題は「原文クリック表示」（ノードクリックで元の文書一覧を表示）のみで、着手は別途。
